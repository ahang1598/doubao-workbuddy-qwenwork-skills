# CHANGELOG · 媒介策略 · flow-media-manager

> 本文件是**历史归档**，不进模型上下文（SKILL.md 只保留生效规则）。
> 阅读须知：历史条目里的做法**可能已被后续版本作废**——判断现行行为一律以 SKILL.md 正文为准，本文件仅用于追溯"某个规则是什么时候、因为什么改的"。
> 归档时间：2026-08-26（当前版本 v1.27.0）

---

> **v1.27.0 改动**（2026-08-26，用户决策：需求单只产出文本 + 灌入合并为一次）：
> 1. 🚨 **输入源回切对话上下文**——ad-demand-helper-pro v2.32.0 起不再灌入金手指（需求单阶段零 MCP），`GET /api/mcp/session/{sid}` 取 `demand.groups` 的做法（v1.25.5～v1.26.x）**作废**，改从对话栏那份立项单**完整文字**逐字段读
> 2. ⭐ **灌入合并为唯一一次**：本环节 `open_config` 必须一次灌全 48 字段 = `demand.groups`（需求单 5 分区，源自立项单文字）+ Part 2/3/4 策略字段（本环节结论）。**不能只灌策略字段**——否则页面第一步「需求单确认」是空的
> 3. 📌 **拉起后的页面 stage 现状（前端源码实证 2026-08-26）**：`index-*.js` 里 `?session=` 与 `#s=` 两条路径都写死 `o(G.PLAN_PREVIEW)`，**与 strategy 内容无关**，`open_config` 也没有 stage 入参 → **调用侧无法直接拉起执行单页**。页面五步枚举：`PLAN_PREVIEW=1`(需求单确认) / `AI_OPTIMIZER=4`(AI投手准备，含「投放策略」「素材策略」两 tab，即执行单) / `UPLOAD_MATERIAL=5` / `GO_LIVE=6` / `DASHBOARD=7`。两个策略 tab 的显示条件（源码函数）：`dc()` = `kpis|fields|deliveryHours` 任一非空 → 显示「投放策略」；`fc()` = `kpis|sellingPoint|specs|poolDesc|testDirections|brandAssets|coreClaim|creativeDirections|finishedSpec` 任一非空 → 显示「素材策略」。**只要我们灌全 48 字段，两个 tab 的数据就都在**，用户点一次「确认」即可进入。
> 4. 🛠 **给金手指侧的精确改法**（反馈清单第 6 条升级）：把两条路径的 `o(G.PLAN_PREVIEW)` 改为 `o( dc(ct)||fc(ct) ? G.AI_OPTIMIZER : G.PLAN_PREVIEW )`——含策略字段就直接落到执行单页，仅需求单才停在第一步

> **v1.26.0 改动**（相对 v1.25.9，配套专家包 5.0.0，2026-08-25 全链路实测复盘）：⭐ 主路径去 HTML 化 + session 存活二次校验
> 1. 🚨 **主路径不再写本地确认单 HTML**（用户实测反馈：拉起金手指页面后又产出了本地版策略单 HTML，冗余）——阶段2 直接「拼 strategy 全量 48 字段 → open_config 灌入 → session 校验 → present_files 拉起」，全程无 HTML 产物；**HTML 仅在金手指 MCP 不可用 / `degraded:true` 时兜底**（存档 + 按既定轨道打开，话术改回"确认后我再灌入"）
> 2. ⭐ **session 存活二次校验（新增）**：灌入成功 ≠ 链接可用——2026-08-25 实测 19:58 灌入成功、19:59 服务端重启清空内存 session、20:01 查询 404；流程改为「open_config → curl 字段校验 → sleep 2 → curl 存活复查（HTTP 200）→ 才 present_files」，复查非 200 → 重新 open_config 重灌（新 session），并向用户如实说明"服务端刚重启过，重新灌了一版"
> 3. ⭐ **兜底链接前置**：右栏预览面板对深链渲染偶发空白（v1.25.1 已记录 iframe 不可靠）——兜底 markdown 超链接从"末尾附带"改为**与 present_files 同一条回复里就给出**（"右栏要是空白的，点这里进配置页"），用户空白时立刻有出口
> 4. **curl 校验命令去掉内嵌 `open`**——旧模板 `... && open "$BASE/pre-launch?session=$SID"` 与"打开一律 present_files"硬规则自相矛盾，agent 照抄命令就跳外部浏览器；命令只做校验，打开永远是 present_files
> 5. **收尾话术/交接摘要保真规则（新增）**：只陈述实际发生的动作——没打开不说"已打开"、没灌入不说"已灌入"、二次校验失败重灌了要如实说；禁止照抄模板里的示例状态
> 6. **反例去数字**：收尾话术反例段里的具体数字（2条广告×3素材/40-50元/素材8条等）全部改占位符——反例本意是批评风格，但 agent 会把里面的数字抄进真实产出
> 7. **素材数量硬约束**：数量必须由段B 预算驱动测算公式算出，**禁止照抄示例/反例/上一项目的数字**（实测发现 agent 直接复用了反例里的"8 条素材"）


> **v1.25.8 改动**（相对 v1.25.7）：专家更名同步——open_config 示例 `demand.footNote` 改为「由金手指·媒介经理生成」（配套 plugin v3.64.0，2026-08-25）


> **v1.25.7 改动**（相对 v1.25.6）：⭐ CTA 规则恢复素材策略专家原始规范（2026-08-24 14:33，用户指正）
> 1. **删除 v1.25.6 臆造的 og 映射表**（"立即预约/免费咨询/立即注册"等文案**不在平台按钮列表里**——是模型编的）
> 2. **恢复原始规范**（素材策略专家 copywriting_method.md「转化按钮」章节）：按钮是**平台固定列表**（朋友圈 17 个：了解更多/去逛逛/查看详情/立即体验/立即咨询/立即购买/立即抢购/立即领取等），**不可自定义**；按**产品调性**选（氛围型→去逛逛；强购买→立即购买；限时→立即抢购；工具试用→立即体验；留资→立即咨询；权益→立即领取；兜底→了解更多）
> 3. **保留硬约束**：选什么按钮，落地页必须能完成对应动作，否则审核驳回（隐含 og 一致性）
> 4. 时段渲染硬规则（v1.25.6 第 1 条）不变

> **v1.25.6 改动**（相对 v1.25.5）：⭐ 时段渲染硬规则 + CTA 按钮推导策略（2026-08-24 14:25 用户实测反馈两个 bug）
> 1. 🚨 **日内时段渲染硬规则**：时段基线在确认单 HTML 里**就是一行文字**（`00-07 低/暂停 ｜ 09-11 放量 ｜ …`）——实测 bug：agent 自由发挥做了"每小时一行"表格且把时段字符串**逐字符遍历**出乱码（"00-07 低/暂停"被拆成 0-0 / 1-0 / 2-0- …）；禁止逐字符 enumerate，`deliveryHours` 传金手指时 key 是时段区间（"00-07"）、值是短语（"低/暂停"），共 4 块左右
> 2. ⭐ **CTA 按钮推导策略表（新增）**：之前 skill 只定义了 `ctaCopy` 字段格式没定义按钮文案怎么推——agent 拍脑袋写出与 og 错位的按钮（og=COMPLETE_ORDER 配"免费预约演示"）。新增强制策略表：按钮按 og 语义对齐（COMPLETE_ORDER→立即下单/了解详情；RESERVATION→立即预约/免费咨询 等 5 行），**铁律：按钮暗示行为必须与 og 语义一致**，错位禁止——先回段A 查 og 判定本身是否错了
> 3. 段 B 补齐 ⑦ 外层文案与按钮汇总完整规范（外层/首评条数公式 + CTA 表）

> **v1.25.5 改动**（相对 v1.25.4）：⭐ 输入 1 数据源切换（2026-08-24 13:25 用户实测反馈）
> 1. **需求立项单不再从对话上下文读**——ad-demand-helper-pro v2.30.0 起对话栏不再重复打 5 分区字段（右栏已是权威源）
> 2. **改为从金手指 session 重新拉**——灌入时的 sessionId 已在内存，调 `GET {BASE}/api/mcp/session/{sid}` 拿 `strategy.demand.groups`（完整结构化字段，对话上下文不再承载 `demand_*` 值）
> 3. **下游策略环节改动极小**——字段名语义不变，只是数据源从"对话文字"换成"session JSON"

> **v1.25.4 改动**（相对 v1.25.3）：⭐ 打开方式硬规则**完全排除 Bash open 选项**（2026-08-24 13:16 用户实测反馈：expert agent 默认走 Bash open 跳外部浏览器，因为之前留了"Bash open 兜底"措辞）
> 1. **打开金手指页面没有第二条路**——`present_files` 传 session 短链是**唯一**打开方式；之前的"Bash open 仅在 present_files 不可用时做兜底"措辞**删除**——这个兜底反而给 expert agent 留了 fallback 退路
> 2. 灌入章节明确"agent 默认动作 Bash open 是 shell 语义陷阱"——必须在 skill 里排除

> **v1.25.3 改动**（相对 v1.25.2）：⭐ 拉起方式硬规则强化 + 灌入后页面行为说明（2026-08-24 用户实测反馈）
> 1. 🚨 **打开一律走 `present_files` 右栏拉起**（session 短链，32 位 hex 零复制风险）——❌ 禁止 `Bash open` 跳系统浏览器（用户实测：跳外会丢失对话上下文，体验差）。`Bash open` 仅在 present_files 完全不可用时做兜底
> 2. **灌入后页面行为说明**：金手指 `PreLaunch.tsx` 的 `setStage` 写死 `PLAN_PREVIEW`（"投放需求"步骤）——所有 open_config 都从需求确认开始，**确认单全量灌入也停在"投放需求"步骤**，需手动点"下一步"进"投放计划"。短期 workaround 已写进 4/4 收尾话术；长期需金手指按 strategy 内容判定 stage（已列入给金手指的反馈清单）
> 3. 给金手指反馈清单追加第 6 条：setStage 按灌入内容判定（全量策略跳"投放计划"，仅需求单跳"投放需求"）

> **v1.25.2 改动**（相对 v1.25.1）：⭐ 打开方式终稿——session 短链 + curl 字段清单校验（2026-08-24 全链路实测跑通）
> 1. **FAT 环境主路径改走 `?session=` 短链**：32 位 hex **零复制风险**——v1.25.1 的解码闸门实测拦住了损坏链接，但也证实 1000+ 字符 URL 在 AI 调用链复制**稳定损坏**（两次 gzip 报废），重试无用；session 短链彻底绕开长字符串
> 2. **curl 字段清单校验（必做）**：`GET /api/mcp/session/{id}` + python 机检 48 字段命中清单——实测抓出过漏传的 `creativeGroupCount`，比肉眼看页面精确；报错分两类处理（过期→重灌 / 漏字段→补齐重灌）
> 3. **deepLink 禁令修订（分环境）**：FAT 单实例放行（先 curl 验证再 open）；生产多副本仍禁（内存 session 查不回）——生产就绪依赖金手指 `inlineChecksum` 或短码模式
> 4. **兜底链接简化**：session 短链 ~90 字符，可直接给 markdown 超链接兜底（不再需要藏长 URL）
> 5. v1.25.1 的解码闸门降级为"生产 fallback 过渡做法"保留
> 6. **UI 统一注记**（同日补）：可行性评估已切换 `html-report-card` 卡片规范（ad-demand-helper-pro v2.27.0）——执行确认单暂仍用旧模板，"与可行性评估 UI 一致"规矩暂停，迁移另行决策

> **v1.25.1 改动**（相对 v1.25.0）：⭐ 灌入链接加"校验闸门" + 修正 #3 误判（2026-08-24 mock 演练 + 金手指源码走读结论）
> 1. 🚨 **长 URL 复制保真风险实锤**：`#s=` 内联深链 1000~3000+ 字符，复制转手丢/错一个字符 gzip 流整体报废（mock 演练实测解码失败 → 页面空白/显示旧缓存草稿）。**不是 URL 超长**（上限 32000），是复制保真概率问题
> 2. **校验闸门（新硬规则）**：URL 只复制一次进 shell 变量，同一条 Bash 命令「python 解码校验 → 通过才 `open`」；校验报错即重调 `open_config` 拿新链接；兜底 markdown 链接必须用校验通过的那份 URL
> 3. ~~**recommendedLink 一律走 Bash open**，不再走 present_files 传 URL~~ 🚫 **本条已于 v1.25.4 作废**（现行硬规则：金手指页面只走 present_files 传 session 短链，绝不 Bash open）——历史记录，勿照做。（原文：内置预览面板 iframe 对 `#s=` 深链不可靠，两次实测空白；HTML 文件产物仍走 present_files）
> 4. ✅ **修正早前误判**：#3 落库缺表**不影响**内联预填——源码证明 `#s=` 是纯客户端解码（`PreLaunch.tsx` decodeInline），与数据库无关。"页面空白"先查 URL 复制损坏，不归因 #3
> 5. **给金手指侧建议**：`open_config` 返回加 `inlineChecksum`（payload sha256 前 8 位），调用方 O(1) 校验

> **v1.25.0 改动**（相对 v1.24.7）：⭐ 3/4 产出即灌入——执行确认单落盘后立即 `open_config` 全量灌入 + 拉起金手指配置页，3/4 与 4/4 合并（用户决策，配套字段规范「两次灌入调用时序」章节）
> 1. **产出即灌入**：执行确认单 HTML 写完落盘（存档）后，**不等用户对话确认**，立即调 `open_config` 灌入全量 48 字段（`demand` + Part 2/3/4 顶层字段）→ 取 `recommendedLink` 打开配置页。用户确认环节**移到页面上**（页面可核对预填、直接改字段）
> 2. **全链路 4 步变 3 步**：提需（含灌入，ad-demand-helper-pro v2.25.0）→ 可行性评估 → 确认单（含灌入+上线）。原"4/4 等用户说确认才灌入"的节奏废除
> 3. **HTML 降为存档产物**：继续 `Write` 落 `投放产出/`（历史可溯 + 长内容阅读舒适），主交互迁金手指配置页
> 4. **30 分钟过期**：`recommendedLink` 返回 `expiresInMinutes:30`——打开时提醒用户尽快点开；过期用户再说"开投/重新灌"→ 重新调 `open_config` 生成新链接（值不丢）
> 5. **重灌规则**：用户在页面核对后回对话要改 → 改完**重新调 `open_config`** 生成新链接（旧 session 自然过期，无脏页面）
> 6. **老链路 fallback**：金手指 MCP 不可用 / `degraded:true` → 退回老节奏（对话确认 + 用户确认后再灌入），不阻塞产出
> 7. **金手指新连接器同步**（2026-08-24，配套隔壁任务对接纪要）：连接器 key 换为 **`jinshouzhi`**（14 工具，MCP 端点 `https://ad-goldfinger.app.fitgroup-fat.com/mcp`，2026-08-26 起需配置 GOLD_FINGER_TOKEN；旧 `ad-goldfinger` 是残留通道）——深链域名已修（不再指旧域）；**`recommendedLink` scheme 为 http，调用侧必须手动替换为 https**（服务端问题 #4）；**落库读写工具全挡**（问题 #3 生产库缺表），仅 `open_config` session 通道可用——延伸章节加了暂不可用注记
> 8. **48 字段灌入实测确认**（隔壁任务第二版重灌，`inlineMode:lite / degraded:false`）：页面读 `{k, v}` 中文键、Part 3/4 是 strategy 顶层字段、`outerCopy`/`firstComments` 数组、`ctaCopy` {primary,backup}——与字段规范一致，"还差 6 项"清零待页面复核
> 9. 配套 `ad-demand-helper-pro` v2.26.0 / `flow-demand-to-media` v3.17.0 / 字段规范契约章节

> **v1.24.7 改动**（相对 v1.24.6）：⭐ 上游删 C1 同步——og 判定链输入源调整
>
> 1. **营销目的输入**：原「C2 投放目标 × C1 产品类型」→ **C2 单独承载**（ad-demand-helper-pro v2.24.0 删 C1，与 C2 重复）；品牌宣传由 C3"品牌声量优先"单信号路由
> 2. **自建载体细分推导**：C1→C2（用户增长→APP/SDK；商品销售/线索留资→H5·小程序/JS）
> 3. **og-library.md 判定链 Step 1/2 输入同步 + 修正开发资源题编号 C12→C14**（v2.22.0 重排后 C12 是素材准备、C14 才是开发资源，历史笔误）

> **v1.24.6 改动**（相对 v1.24.5）：⭐ Part 4 字段拆三
> 1. **`copyDeck` 拆为 3 个独立字段**（用户决策）：`outerCopy`（外层文案 string[]）/ `firstComments`（首条评论 string[]）/ `ctaCopy`（转化按钮文案 object {primary, backup}）——避免一个 object 混三项的字段名/层级歧义
> 2. **JSON 骨架同步**：删 `copyDeck` object 块，改为 strategy 顶层 3 字段
> 3. **字段规范 Part 4 同步**：1 字段 → 3 字段，统计 47 → 49
> 4. 配套专家包 v3.50.0

> **v1.24.5 改动**（相对 v1.24.4）：⭐ 字段重命名——`materialTypes` → `creativeDirections`
> 1. **Part 3.3 #3 字段名更名**（用户决策，单字段不分拆）：`materialTypes` → `creativeDirections`（素材方向，每个素材的创作方向/角度）；中文名"素材类型"→"素材方向"
> 2. **结构不动**：仍为单字段 object[]，每项结构 `{category: "image"|"video", type, priority, count, size, message, narrative?}` 保留；category 分流图片/视频不变
> 3. **金手指第三步对齐**：金手指素材类型也按"图片/视频分开 + direction 类型"组织（v1.24.3 即此结构）
> 4. 配套字段规范 3.3 #3 + JSON 骨架同步

> **v1.24.4 改动**（相对 v1.24.3）：⭐ 专家职业名更名同步
> 1. **专家职业名改为「金手指·媒介经理」**（原 profession"金手指 · 广告投放专家"，v1.13.1 设定）：open_config 示例 `demand.footNote` 改为"由金手指·媒介经理生成"；1.13.1 历史 changelog 加更名注记
> 2. 配套 flow-demand-to-media v3.16.0（专家入口更名）/ ad-demand-helper-pro 同步

> **v1.24.3 改动**（相对 v1.24.2）：⭐ 字段规范删减 4 个灌入字段 + Part 3.3 两字段重命名细化
> 1. **删 4 个灌入字段**（用户决策）：`placements`（主投版位——智投场景系统择优无预设值）/ `refreshCadence`（上新节奏）/ `watchMetrics`（盯盘指标）/ `bidAdjustRule`（调价节奏）——后三者为运营指导类内容，不作为灌入字段，仅在确认单 HTML 呈现。open_config 示例删 `placements` 行并加注释
> 2. **Part 3.3 重命名**：图片创意类型（③）→ **素材类型**（Key 改 `materialTypes`，加 `category: "image"|"video"` 分流，视频 4 类后续增补）；文案与按钮汇总（⑦）→ **外层文案与按钮汇总**（取值规范细化为三部分：外层文案/首条评论/转化按钮文案，避免与素材文案混淆）
> 3. 配套字段规范统计更新：Part 2 已承载 18→17（删 placements）、🆕 9→6（删三个运营字段），全量 53→49

> **v1.24.2 改动**（相对 v1.24.1）：⭐ 兜底计费同步策略——点击 og 配 CPC 出价（而非 oCPM/oCPX）
> 1. **兜底链路补全**：无归因场景下，og=AD_CLICK + **bidding=CPC**（按点击计费、不优化转化），不是 oCPM/oCPX——oCPX 链路需要归因上报，没归因接不进去
> 2. **出价联动校验二分**：
>    - 选转化 og → bidding 必须为 oCPX（与 `optimizationGoal` 交叉校验，矛盾即报）
>    - 选点击 og（兜底）→ bidding 必须为 CPC（按点击计费）
> 3. **og-library.md 同步**：判定链 Step 5 增 CPC 兜底分支、推荐位表"点击 og"行更新为"点击 og + CPC 出价（兜底）"、话术补充 CPC 描述
> 4. 配套字段规范 2.2 #7 同步

> **v1.24.1 改动**（相对 v1.24.0）：⭐ 考核红线字段回补（独立指标，不成对）
> 1. **回补 `kpiRedline` 字段**（独立于 `kpi`，平级而非成对）：`kpi` = 考核目标（如 `成本≤90元` CPA 上限），`kpiRedline` = 红线值（如 `成本≤120元`）—— 两者是**两个独立指标**，不是双指标成对关系
> 2. **推导规则**（按指标类型分方向）：
>    - 成本类：红线 = 常规 × 1.2-1.3 上浮（如 `成本≤90元`→`成本≤120元`）
>    - ROI/量/率类：红线 = 常规 × 0.7 下浮（如 `ROI>1`→`ROI>0.7`）
> 3. **触发动作暂不建设**（触碰红线后动作另行），字段值先录入
> 4. 配套字段规范 2.2 #9 + JSON 骨架

> **v1.24.0 改动**（相对 v1.23.1）：⭐ og 判定逻辑重写为官方口径 + 新增 og 枚举库（只要层1 浅层，不要深转、不要行业）
> 1. **新增 `references/og-library.md`**：og 枚举库——**只做层1 语义路由层**（33 个浅层 og，按营销目的五组：商品销售/线索留资/加粉互动/用户增长/品牌兜底），每个带判定标签（载体/归因/门槛）；**不含深度转化目标**（ROI/次留/变现类，用户决策）、**不含行业特化 og**（金融/教育/短剧/网文类，涉及时调知识库查）
> 2. **判定链从"开发资源单变量"重写为官方口径**（v1.23.0 的粗判定作废）：严格按官方四元组联动 `营销目的→推广产品→营销载体→优化目标`（promotion_product_basic/deep.md）+ **归因硬门槛**（attribution_basic.md："不接归因=不能用优化目标投放"）：
>    - 归因判定看**上报方式**不看"有无开发"——微信小店/视频号小店=**平台自动归因无需开发**；落地页=JS；App=SDK/API；无归因=点击/曝光兜底
> 3. **推荐位**：优先推荐平台自归因路径（微信小店，前提=用户自行搭建官方落地页），点击 og 只做兜底并附升级建议话术
> 4. **出价联动校验**：选转化 og → 计费只能 oCPX（attribution_basic.md），与段A `bidding` 交叉校验
> 5. **枚举防编造**：官方 `action_type` 全量 150+（2026-08-23 核对 user_actions/add 文档），判定链管语义路由、平台下拉管最终可选；`ACTIVATE_APP` 修正知识库旧记 `ACTIVE`
> 6. 配套字段规范 2.2 #7/#8 同步

> **v1.23.1 改动**（相对 v1.23.0）：⭐ kpi 回退单指标（用户复核决策）
> 1. **kpi 双指标逻辑删除**：`kpiRedline`（红线/止损线）字段整体删除——不再区分常规/红线
> 2. **kpi 单指标、只用成本口径**：`kpi` = 考核成本上限，如 `成本≤90元`（CPA 上限），透传自需求单成本预期；出价公式（考核成本×50%-70%）与段B 数量测算公式（广告数=日预算÷CPA×20）均以该 CPA 为输入
> 3. **触发动作链路暂不建设**（红线停投/预警分级等），后续需要再议

> **v1.23.0 改动**（相对 v1.22.0）：⭐ 段A 三处字段修正——投放方式去重 / ~~kpi 拆双指标~~（v1.23.1 已回退） / 新增优化目标 og 双字段
> 1. **投放方式与是否智投去重**：`isSmartDelivery` 与 `deliveryMode` 语义重复——`isSmartDelivery` 不再作为独立取值字段，由 `deliveryMode` 自动派生（智投=true / 标准=false）；open_config 灌入时派生后照传（金手指前端分支用），字段规范侧只维护 `deliveryMode` 一个
> 2. ~~**kpi 拆双指标（常规 + 红线）**~~（**v1.23.1 回退**：改单指标、只用成本口径，`kpiRedline` 删除）
> 3. **新增「优化目标 og」双字段**（建单必填，前置依赖链路）：
>    - 字段：`optimizationGoal`（og 英文枚举）+ `optimizationGoalName`（og 中文名），**展示默认英文名+中文名，分拆双字段**
>    - **前置依赖**：上游立项单 `demand_dev_resource`（开发资源，ad-demand-helper-pro v2.21.0 起新增提问"公司有开发资源吗"）
>    - **判定**：调知识库（`tencent-ads-delivery-guide`）依据开发资源回答判定——**有开发**（可对接转化归因上报）→ 深度转化 og（转化/ROI 类）；**无开发** → 浅层 og（点击等，无需技术对接）。具体枚举以知识库/腾讯广告 API 为准，不凭印象编
> 4. 配套 ad-demand-helper-pro v2.21.0

> **v1.22.0 改动**（相对 v1.20.0）：⭐ 段B 数量测算新增「基建驱动公式」（预算→基建→素材/文案数量一条线）
> 1. **四项计算公式**（落在段B ③数量测算章节，产出可执行数字）：
>    - **广告数 = 日预算 ÷（CPA × 20）**——每条广告日消耗达 CPA×20 才能积累足量转化，此式算出日预算能养活的并行广告条数（结果取整，不足 1 条按 1 条）
>    - **素材数 = 广告数 × 3-5**——每条广告配 3-5 素材，与段A 广告结构分档一致
>    - **外层文案数 = 广告数**——每条广告 1 条外层文案
>    - **首条评论数 = 广告数**——每条广告 1 条首条评论（朋友圈版位）
> 2. **首次投放硬约束**：素材储备**不低于 30**（基建路径首投场景，此下限取代原"最低 10 条"）
> 3. **口径区分**：在投素材（广告数×3-5）是当期消耗量；素材池储备（首投 ≥30）是轮换余量——`materialPool.total` 按储备口径填，两者别混
> 4. 落点：段B 外层文案与按钮汇总表（⑦，v1.24.3 起改名）的外层文案/首条评论数量**按公式动态算**，不再固定"约 10 条 / 约 5 条"
> 5. 原预算互斥档表保留作总量快速参照，精确数字以公式为准

> **v1.20.0 改动**（相对 v1.19.0）：⭐ 段A 补五维基建分档推导（依据《金手指段A_vs_AI投手五维基建_精细对比与优化点》）（plugin v3.36.0）
> 1. **根因**：段A 落地时把 AI投手五维方法论过度简化——分档逻辑（账户 4 档、结构 3 档）、出价公式、定向默认值、时段基线全部丢失，只剩"默认 1 白户 + 通用裸区间 + 裸出价"
> 2. **🔴 必改 4 项全落地**：
>    - **账户数分档推导**（日预算 4 档：<1000→1 / 1000-5000→1-2 / 5000-20000→2-3 / >20000→3-5），不再一律"默认 1 白户"
>    - **广告结构分档**（日预算 3 档：<2000→3×3×5 / 2000-10000→3-5×3-5×5-10 / >10000→5×5×10），字段值给分档结果不给裸区间
>    - **出价公式**：初始出价 = 考核成本 × 50%-70%，字段值附推导小字 + 调价节奏（≤10%/观察4h/冷启动别频繁调/5-10转化后回调）
>    - **定向方式默认值**：系统智能定向（首选），有老客种子叠加 lookalike
> 3. **🟡 建议改 3 项全落地**：素材池储备字段（衔接段B）/ 投放时段基线（00-07 低、09-11 放量、14-17 观察、20-23 高峰）/ 上新节奏（每 3-5 天补 1-2 条）
> 4. **🟢 可选 4 项全落地**：KPI 卡扩展 6 个（加素材池总量+账户分档）/ 投放方式格式固定（skill 名括号附后）/ doc-head 路径判定附依据 / 盯盘节奏加"盯指标"（出量占比/消耗分布/素材跑速/红线预警/冷启动进度）
> 5. **🐛 Bug 2 项全修**：
>    - 段B 档位表改互斥：`<1000 / 1000-5000 / 5000-10000 / >10000`（旧表 ≤500/~5000/500-1万 边界重叠，6000 元无法判定档）
>    - open_config 示例 `materialPool` 改动态值 + 注释"按段B 测算结果填"（旧示例 {50,10,40} 是已废弃固定配置，诱导回退）
> 6. **边界澄清**：分档推导小字属"策略可施工内容"（保留），与"待办型警告"（禁止）不同——两者别混
> 7. 交接摘要"前置提醒 alert"已被 v1.19.0 删除，v1.20.0 不恢复
> 8. 文档中"account_buildup_strategy.md 同步反向"属 AI投手侧改动，不在本包范围
> 9. 配套 plugin v3.36.0

> **v1.19.0 改动**（相对 v1.18.0）：⭐ 段A 末"投放前需提供" alert 删 + 段B 品牌资产透传 block 删"建议联系"待办型话术（plugin v3.32.0）
> 1. **段A 末"投放前需提供" alert 删**：v1.18.0 多留的"进 ai投手投放前需提供（账户ID+落地页URL）"橙色 alert 本质是**待办型警告**——越界。**HTML 模板里删了**这个 alert div
> 2. **段B 品牌资产透传 block 删"建议联系"话术**：
>    - 模板里"→ 素材助手需直接读取《需求立项单》...本栏仅作透传指引"删（这是"告诉下游怎么做"的指导性话术）
>    - "缺失项用 pill miss 标记"——只标记状态，**不附"建议联系设计/品牌方"等待办型话术**
>    - SKILL.md 同位置（段B ⑥ 品牌资产透传章节 + 前置条件章节的兜底提示"若无 logo：可用素材图裁 512×512 占位兜底，但建议联系设计/品牌方补充正规品牌资产"）全删
> 3. **agents md 同步**：「品牌资产缺失时标注'建议联系设计/品牌方'」改"只标记状态"；「品牌资产未收集，建议联系设计/品牌方」全删
> 4. **flow-demand-to-media 同步**：「品牌资产若缺失 → 素材策略段标注'建议联系设计/品牌方'」全删
> 5. **模板一致性自检**加严：v1.18.0 的"段A末不输出待办型警告"扩展为含"投放前需提供/进 ai投手投放前需提供"；新增"段B 品牌资产透传不附待办型话术"检查项（覆盖"建议联系设计/品牌方/请以需求单为准/本栏仅作透传指引/512×512 占位兜底"等关键字）
> 6. **核心原则重申**：执行单只交付"策略 + 可施工内容"，**不输出"该怎么做"的话术**——用户怎么补资产、怎么上传落地页、怎么开户，都不属于执行单职责
> 7. 配套 plugin v3.32.0

> **v1.18.0 改动**（相对 v1.17.0）：⭐ 删除段C 投手执行手册 + 段A 末禁止输出待办型警告（plugin v3.30.0）
> 1. 🚨 **删除段C 整个段**——投手执行手册（仅上传+建创意 SOP）越界：那是 ai投手 自身的职责，不归媒介经理管。SKILL.md 段C 整段删除；模板 `.card.seg-C` div 删；段导航"投手执行手册"链接删；交接摘要卡从 3 张变 2 张
> 2. 🚨 **段A 末禁令**：**不输出**任何"针对可行性评估风险的处理" alert / 账户未配置项的"前置条件配置指引" / "合规文案自检" / "上线前必做审核预检"等**待办型警告**——这些是越界的"上线前待办清单"，应交给用户按段A「盯盘节奏」+金手指"项目管理→腾讯广告 Key 托管"自行检查。**执行单只交付"策略 + 可施工内容"**
> 3. **可行性评估 ⚠️/❌ 项标注迁移**：v1.2 起的"段A/C 标注"规则更新——段C 删了，标注**全部由段A 字段承担**（融在字段值里，不开新 alert 块）
> 4. **接口契约表更新**：「转化归因配置提醒」和「上传 SOP」两行删（属越界，砍）
> 5. **html-memo-template 同步**：段A 末"前置条件 alert"占位符删；段C div 删；填写指南"段 C：投手执行手册"整段删；一致性自检加 2 条硬检查（"不出现 .card.seg-C"+"不出现待办型警告关键字")
> 6. **flow-demand-to-media / agents md / README / plugin.json description** 同步：3 段 → 2 段表述
> 7. 描述**保留** v1.2 的"针对性标注"逻辑（v1.2 规则保留，只是落到段A）
> 8. 配套 plugin v3.30.0

> **v1.17.0 改动**（相对 v1.16.0）：⭐ 段B 按金手指「素材策略专家」规范重写（plugin v3.29.0）
> 1. **规范来源**：material-strategy-assistant v3.2 brief_spec 的**策略层**（选类型/做多少/要不要视频/怎么传信息）；逐条文案、生图提示词、PDF 交付等执行层**不进本链路**（下游素材模块职责）
> 2. **数量测算改预算驱动**（替换旧"固定 50 条 = 10 视频 + 40 图文"）：≤500→约10张 / ~5000→约20张 / 500–1万→10-50张 / >1万→按账户数 50张/账户封顶；**最低 10 条**硬约束
> 3. **视频门槛收紧**：仅 **>1 万元或点名视频/投视频号**才规划视频（旧版默认带 10 条视频，已废）；满足门槛时规划"哪几类（情景剧/口播/动图/混剪）、各几条、为什么 + 叙事套路"
> 4. **新增创意类型体系**：图片只能在**官方 13 种类型**里选 2-3 类（严禁自创/改名），每类配「怎么给用户传信息」大白话；内部选型黑话（海报类/原生类/平台样式类/业务元素类）**绝不外显**
> 5. **尺寸按类型定横竖方**（替换一刀切）：榜单/大字报/口播等竖 9:16，海报/公众号资讯等横 16:9，兜底方图；整批横竖方约 4:4:2
> 6. **新增「核心主张」**：一条加粗主张 + 判断理由，放段B最前（单一聚焦）
> 7. **删除**「8大测试分类维度」测试方向（新规范无此概念，由优先级替代）；**保留**一句话卖点透传、品牌资产透传（缺口C）、成品裁剪压缩规格
> 8. html-memo-template 段B填写指南同步重写（核心主张卡/融合表/视频规划块）；agents md 能力第 4 条同步
> 9. 配套 plugin v3.29.0

> **v1.16.0 改动**（相对 v1.15.0）：⭐ 4/4 改走**纯结构化字段**，不再灌 HTML（plugin v3.27.0）
> 1. 🚨 **不再传 `rawDemandHtml` / `rawHtml`**：改为 `strategy` 里直接给结构化字段——`demand.groups`（需求单）+ 23 个策略字段（offerName/dailyBudget/bidding/materialPool 等）
> 2. **原因**：HTML 通道要页面用 DOM 选择器（`.sec` `.field` `.k` `.v`）反解析回结构——**我们改模板样式就可能解析失败**；而配置页前端本就有结构化分支，字段直给零解析风险
> 3. **已抓前端 JS 核实**：需求单 `rawDemandHtml||demandHtml` 不是 HTML 时读 `demand.groups`；策略单 `rawHtml||html` 不是 HTML 时走纯结构化分支（23 字段全列）
> 4. **实测验证**：`success:true` / `degraded:false` / `demand.groups` 4 分区完整 / payload 无任何 HTML 字段
> 5. ⚠️ **`inlineMode:lite` 是正常的**（`full` 仅指含 HTML 还原）——结构化方案本就无 HTML，**不要向用户提示"降级"**；只有 `degraded:true` 才是真降级
> 6. 上游 `ad-demand-helper-pro` v2.16.0 同步删掉"需求单静默 HTML"，少写一个文件
> 7. 配套 `ad-demand-helper-pro` v2.16.0 / `flow-demand-to-media` v3.13.0 / plugin v3.27.0

> **v1.15.0 改动**（相对 v1.14.0）：4/4 灌入两份 HTML（⚠️ **已被 v1.16.0 取代**——改走纯结构化字段）

> **v1.14.0 改动**（相对 v1.13.1）：⭐ HTML 顶部加金手指 logo（plugin v3.25.0）
> 1. **`references/html-memo-template.md` topbar 左侧改为金手指 logo**：图标 base64 内嵌（用户提供的蓝色版原图裁切）+ "金手指 / AI · Ad Agent" HTML 文字 + 竖线分隔，替换原来的 🎯 emoji
> 2. **新增 `references/gf-logo-base64.txt`**（base64 文本，写 HTML 时读它替换 `{{GF_LOGO_BASE64}}` 占位符）+ `gf-logo-icon.png`（源图留档）。⚠️ 不许自己画 SVG、不许引本地路径——HTML 必须自包含
> 3. **topbar 排版修正**（用户实测反馈"下面的文字排版不太行"）：`.topmeta` 加 `margin-left:auto` + `align-items:flex-end` 宽屏右对齐；新增 ≤760px media query，窄屏换行后靠左 + 细线分隔，**不再居中错位**
> 4. **设计系统表 + 一致性自检加 logo/排版条目**：每份 HTML 必带 logo 且 base64 已注入
> 5. 配套 `ad-demand-helper-pro` v2.14.0（评估模板同款 logo）/ plugin v3.25.0

> **1.13.1 改动**：display_name 改名（原"媒介经理·串联版"）——配合专家 profession 改为"金手指 · 广告投放专家"（plugin v3.23.0）【注：v1.24.4 起 profession 更名为"金手指 · 腾讯广告媒介经理"】


> **v1.13 改动**（相对 v1.12.0）：链接兜底方式修正（用户反馈"还是给我一个链接兜底？只是不要这么长"）
> 1. **v1.12 矫枉过正**：完全禁贴链接——但用户需要兜底（预览面板打不开时自己点链接进配置页）
> 2. **修正为 markdown 超链接兜底**：预览/浏览器为主路径，对话末尾必给一行 `打不开？[点这里进配置页](完整URL)`——**短文字做显示、几百字符的 URL 藏在链接目标里**，对话栏不再出现裸 URL
> 3. 配套 plugin v3.21.0

> **v1.12 改动**（相对 v1.11.0）：🚨 禁贴链接原文
> 1. **用户实测反馈**："这是干嘛...这么长一串链接"——金手指 `recommendedLink` 是内联深链（策略 gzip 内嵌在 `#s=` 里），模型把原文贴在了对话里
> 2. **硬性规则**：拿到的 `recommendedLink` 必须在 `present_files` 或 `Bash open` 里打开——**绝对不要把链接原文贴到对话栏**
> 3. **4/4 收尾话术微调**："金手指配置页打开了" → "金手指配置页打开了，在[右边栏/浏览器里]"——明示走预览/浏览器，不是给链接
> 4. 配套 plugin v3.20.0 / agent md（"禁贴 URL/路径原文"上升为全局对外话术原则）

> **v1.11 改动**（相对 v1.10.0）：⭐ 串联金手指 MCP，4/4 开启投放落地
> 1. **4/4 真实实现**：用户确认执行单后调金手指 `open_config`——`strategy`（执行单结构化字段）+ `rawHtml`（执行单 HTML 全文，页面精确还原）→ 取 `recommendedLink` 按既定轨道打开 → 用户在金手指页面完成建账户/生成投手/上传素材/项目上线
> 2. **链接安全规则**：只呈现 `recommendedLink`；绝不给 `deepLink`（?session= 那条，生产环境空页）
> 3. **4/4 收尾话术**（新格式）：🚀 配置页已打开 + 执行单已灌入 + 页面待办列表 + 后延伸（看数据/调整直接说）
> 4. **4/4 后延伸**：get_project_data 拉真实数据 / adjust_project 调整（写操作必须复述确认后才 confirm:true）
> 5. **异常处理**：金手指未连接 → 提示连接不硬调；鉴权失败 → 引导 Key 托管配置；报错如实告知不编造
> 6. 3/4 收尾话术 🔧 区改为"确认后打开金手指配置页"（原"需要你给账户ID/落地页URL"——这些改在金手指页面完成）
> 7. 配套 plugin v3.19.0（plugin.json 已声明 dependencies.connectors: ["jinshouzhi"]）

> **v1.10 改动**（相对 v1.9.0）：文本格式硬约束
> 1. ⭐ **按 agent md 新文本格式要求重写确认单收尾话术**：🎯 + 📌大体打法（列表） + ⚠️风险已处理（列表） + 🔧你需要给（列表） + 方向确认短句
> 2. "大体打法"从"一句话"改为列表化（3 条短句），更易扫读
> 3. 配套 `ad-demand-helper-pro` v2.12.0 / plugin v3.17.0



> **v1.9 改动**（相对 v1.8.0）：产物收尾按受众分层重写
> 1. ⭐ **核心认知**：投放执行确认单是给投放系统/机器读的，不是给用户读的——专业内容（oCPM/版位/归因）用户不用看懂，收尾话必须主动说清这一点
> 2. **收尾话术升级为硬性步骤**（🚨 实测发现漏说用途——不能只丢产物就走）：必含 (1) 这是给系统读的 (2) **一句话白话打法**（版位+预算节奏+素材打法三要素翻成人话）(3) 用户还缺什么 (4) 方向确认引导
> 3. **"一句话白话打法"提炼标准**：用户看了能向老板转述"我们要怎么投"才算合格；术语复述（"oCPM 出价 80-120……"）不合格
> 4. **不引导用户看执行细节**：不说"你看看出价对不对"——用户只确认方向
> 5. 配套 `ad-demand-helper-pro` v2.11.0 / plugin v3.15.0

> **v1.8 改动**（相对 v1.7.1）：进度条每轮显示 + 话术去标签化
> 1. **进度条改为每条回复开头固定显示**（3/4、4/4 阶段）
> 2. **产物收尾话术去标签化**：禁用"这是什么：/给谁用："，改自然人话
> 3. 配套 `ad-demand-helper-pro` v2.10.0 / plugin v3.14.0

> **v1.7 改动**（相对 v1.6）：白话命名 + 产物说明
> 1. **产物改名**：《投放策略纪要》→《投放执行确认单》
> 2. **每份产物后附一两句自然收尾话术**（禁用标签格式）
> 3. 文件名同步：`投放执行确认单-[项目名].html`
> 4. 配套 `ad-demand-helper-pro` v2.8.0 / `flow-demand-to-media` v3.9.0 / plugin v3.11.0

> **v1.6 改动**（相对 v1.5）：配套上游模板删除
> 1. 上游 ad-demand-helper-pro v2.7.0 删除了立项单 HTML 模板（实测发现模板诱导模型把立项单写成文件）。本 skill 无需改动，仅升版本对齐
> 2. 配套 `ad-demand-helper-pro` v2.7.0 / `flow-demand-to-media` v3.8.0 / plugin v3.10.0

> **v1.5 改动**（相对 v1.4）：呈现方式按《WorkBuddy 专家开发规范》修正
> 1. ⭐ **从"只走 present_files"改为一次定型双轨**：规范 4.2 明确专家环境没有 present_files 工具（只有 Read/Write/Grep/Glob/Bash/WebSearch/WebFetch 等），Bash 一定有
> 2. **规则**：沿用本会话已定型的轨道（阶段 1 的可行性评估产出时已试过一次 present_files 并定型）——present_files 轨道就推卡片（右侧栏），Bash open 轨道就开浏览器；不再重试、不摇摆
> 3. **产物目录迁移**：从 `~/投放策略/` 挪到工作区 `投放产出/[YYYY-MM-DD]/`，与可行性评估同目录
> 4. 配套 `ad-demand-helper-pro` v2.6.0 / `flow-demand-to-media` v3.7.0

> **v1.3 改动**（相对 v1.2.1）：
> 1. ⭐ **立项单不再是文件**：ad-demand-helper-pro v2.4 起立项单改为对话栏文字输出，本 skill **从对话上下文取 `demand_*` 值**，不再 Read 立项单文件
> 2. 可行性评估仍是 HTML 文件，仍按路径 Read
> 3. UI 一致性基准从"立项单 HTML 模板"改为 `references/html-memo-template.md` 自身（立项单已无 HTML）
> **v1.2 改动**（相对 v1.1）：
> 1. 上游从 1 份确认单（3 part）改为 2 份（立项单 + 可行性评估）
> 2. 字段读取按 `demand_*` Key 取值，不依赖外部容器结构
> 3. 段 A/C 针对性标注可行性评估的 ⚠️/❌ 项
