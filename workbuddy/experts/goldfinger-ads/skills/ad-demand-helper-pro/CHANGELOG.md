# CHANGELOG · 需求立项助手 · ad-demand-helper-pro

> 本文件是**历史归档**，不进模型上下文（SKILL.md 只保留生效规则）。
> 阅读须知：历史条目里的做法**可能已被后续版本作废**——判断现行行为一律以 SKILL.md 正文为准，本文件仅用于追溯"某个规则是什么时候、因为什么改的"。
> 归档时间：2026-08-26（当前版本 v2.32.0）

---

> **v2.32.0 改动**（2026-08-26，用户决策：需求单只产出文本确认）：🚨 **删除产出 1.5「灌入金手指需求确认页」整章**——需求单阶段**不调任何 MCP**、不提连接器、不提授权，只在对话栏产出立项单**完整文字**。
> 1. **对话栏回归完整版**：v2.30.0 的"极简版（右栏已展示）"作废——没有右栏了，立项单文字重新成为**唯一权威源**，5 分区里所有"问过"的字段必须打全
> 2. **下游取数源改回对话上下文**：flow-media-manager 不再 curl 金手指 session 取 `demand.groups`（那个 session 已不存在），改从对话上下文读立项单文字
> 3. **灌入合并为一次**：需求单字段 + 策略字段由 flow-media-manager 在投放执行策略环节**一次性灌入**（原两次灌入 → 一次）
> 4. **连接器自检推迟**：需求单阶段不依赖 MCP，"金手指还没授权"的提示推迟到策略灌入节点前才出

> **v2.31.4 改动**（相对 v2.31.3，配套专家包 5.0.0，2026-08-25 全链路实测复盘）：⭐ session 存活二次校验 + 打开方式统一 + 速查表勘误
> 1. ⭐ **session 存活二次校验（新增）**：产出 1.5 灌入后「curl 字段校验 → sleep 2 → curl 存活复查（HTTP 200）」双重校验——2026-08-25 实测灌入成功后服务端重启清了内存 session，3 分钟后 404；复查非 200 → 重新 open_config 重灌并如实告知用户
> 2. **打开方式统一为 present_files（右栏拉起）**——旧措辞"一律 Bash open，不走 present_files"作废：它与本 skill 自己的话术"已在右栏打开"、专家层 present_files 唯一制（flow-media-manager v1.25.4 起）直接矛盾，agent 读到哪条算哪条；curl 命令里的内嵌 `&& open` 同步删除（校验命令只做校验）
> 3. **兜底链接前置**：右栏预览对深链偶发空白——兜底 markdown 超链接从话术末尾改到与 present_files 同一条回复的首行（"右栏要是空白的，点这里进需求确认页"）
> 4. **保真规则（新增）**：话术只陈述实际发生的动作，禁止照抄模板示例状态
> 5. **速查表勘误**：轮次结构"11 道选择题 3 批串行"为旧稿残留，改回 **13 题 4 批**（与正文步骤 2、description 一致）


> **v2.31.2 改动**（相对 v2.31.1）：专家更名同步——footNote 示例与金手指模板 brand-tag 描述改为「金手指·媒介经理」（配套 plugin v3.64.0，2026-08-25）


> **v2.31.1 改动**（相对 v2.31.0）：⭐ 可行性评估分值口径全局切换为 100 分制（用户定稿，2026-08-24 灵感模版 mock 实测确认）
> 1. **总分 [X]/100**（五维各 0-20 分加总），及格线 60/100（12/维）——原 50 分制（各 0-10、及格线 6.0/维）全部作废
> 2. **策略基调换算**：80-100 标准打法 / 50-79 保守打法 / <50 先补齐再投（透传下游同步）
> 3. **状态 tag 阈值**：≤11 bad / 12-13 warn / ≥14 ok
> 4. **banner 完整复刻规则重申**：可行性评估 HTML 一律从金手指专用模板（`html-report-card/assets/template-jinshouzhi.html`，logo 已预嵌入）复制完整 banner 组件（banner-logo + banner-brand + subtitle + icon meta 行），**禁止手写简版 banner**（2026-08-24 mock 实测踩坑：手写 banner 丢 logo）

> **v2.31.0 改动**（相对 v2.30.0）：⭐ 新增「直达模式」（信息前置，灵感模版链路专用，2026-08-24）
> 1. **直达模式（DIRECT_MODE=feasibility）**：上游编排 skill 传入直达标志且用户首条消息已带结构化投放信息时——跳过 4 项前置填空与 13 题串行提问，直接进可行性评估产出
> 2. **补问上限 1 轮**：营销目的/转化载体/预算档位三类关键字段缺失时仅发 1 轮 AskUserQuestion（≤4 题），其余字段从首条消息推导（推导不出按中性档处理并在评估中标注）
> 3. **边界**：直达模式不输出立项单文字、不调 open_config 灌金手指——只产出《投放可行性评估》HTML（html-report-card 规范，7 段结构不变）并 present_files 交付，结尾提示可继续走完整流程
> 4. **不外显**：直达模式不设触发词、不进问题速查表；仅当上游显式传 DIRECT_MODE=feasibility 或用户消息同时满足「明确要可行性评估」+「已给出产品与≥2项投放要素」时进入，普通对话一律走原 3 步收集流程

> **v2.30.0 改动**（相对 v2.29.0）：⭐ 对话栏输出精简（2026-08-24 13:25 用户实测反馈）
> 1. **对话栏不再重复打 5 分区字段文字**——右栏已展示完整可编辑版本，对话栏文字是冗余（实测反馈）
> 2. **产出 1 对话栏简化为**"📋 投放需求单 · [项目名] 已在右栏打开"+ 兜底链接
> 3. **`demand_*` 字段值不再从对话上下文取**——下游 flow-media-manager 改从金手指 session 重新拉（v1.25.5），对话上下文不再承载字段值
> 4. **金手指页面是权威源**——用户要改字段直接页面改即可

> **v2.29.0 改动**（相对 v2.28.0）：⭐ 拉起方式硬规则强化（2026-08-24 13:16，用户实测反馈 expert agent 仍走 Bash open）
> 1. **产出 1.5 话术统一改为"在右栏打开"**——之前写"打开了"会让 agent 默认走 Bash open（shell 语义里"open URL=系统浏览器"），用户实测就跳了外部浏览器
> 2. **话术里加 ⚠️ 禁止**：不写"在浏览器里打开了"、不用 Bash open——金手指页面只走 `present_files` 右栏，没有第二条路
> 3. 删除历史 changelog 里所有"Bash open / 浏览器里打开"作为"打开方式"的措辞（旧 v1.x 一次定型双轨表述作废）——避免模型从历史 changelog 学到 fallback 选项

> **v2.28.0 改动**（相对 v2.27.0）：⭐ 模板预嵌入金手指 logo + 强化产物规范（2026-08-24 用户实测反馈）
> 1. **金手指专用模板** `template-jinshouzhi.html`（v2.28.0 新增）——**已预嵌入金手指 logo（base64 17K 字符）+ brand-tag 全部填好**；agent 现场**不需要再处理 base64 / logo**，直接抄模板只填内容占位符——根本上避免：① Write 工具对超长 base64 字符串截断（实测 bug：base64 太长被截断，logo 显示不全）② AI 试图用 python 脚本拼 HTML 时写出 .md 调试文件污染产物栏
> 2. 🚨 **禁止在产物目录写 .md 调试/组装文件**——所有组装操作都在 `python3 -c '...'` 单条命令里完成
> 3. CSS 加 `.banner-logo`（36×36，左上角，8px 圆角白底）

> **v2.27.0 改动**（相对 v2.26.2）：⭐ 可行性评估 UI 模板切换为 `html-report-card` 统一 HTML 卡片规范（用户指定，2026-08-24）
> 1. **旧模板废弃删除**：`references/feasibility-template.md`（v2.13 引入的 #378ADD 设计系统）整个删除——v3.8 教训：模板存在会诱导模型用旧模板
> 2. **新模板**：专家包 `skills/html-report-card/`（16 条硬规则 + 9 个组件配方 + check_html.py 自检，品牌已配置「金手指 · 腾讯广告媒介经理」，主色默认蓝系）；theme.css 必须全文内联进 template.html，单文件离线可看
> 3. **五维打分组件映射**：五维评估用 `.data-table`（维度/得分/状态 tag/白话），总分+大结论放首个 section（tag-ok/warn/bad 字色），**CSS 进度条废弃**；风险清单按规则 12 用 plain-list
> 4. **交付闸门**：生成后跑 `scripts/check_html.py` 必须 0 error 才 present_files
> 5. **7 段内容结构不变**：头部/总判断/段A 画像/段B 五维/段C 建议/风险/避坑的内容定义全部保留，只是 UI 层换组件

> **v2.26.2 改动**（相对 v2.26.1）：打开方式终稿——session 短链 + curl 校验（与 flow-media-manager v1.25.2 同步，2026-08-24 全链路实测跑通）
> 1. **FAT 主路径改走 `?session=` 短链**（32 位 hex 零复制风险）+ `curl /api/mcp/session/{id}` 校验（提需只查 offerName + demand 分区数）；`recommendedLink`（`#s=` 内联）弃用——长 URL 在 AI 调用链复制稳定损坏
> 2. 兜底链接简化：session 短链可直接给 markdown 超链接
> 3. 完整规则（deepLink 分环境、生产 fallback、inlineChecksum 建议）统一维护在 flow-media-manager《灌入金手指》章节

> **v2.26.1 改动**（相对 v2.26.0）：灌入链接加"校验闸门" + 修正 #3 误判（2026-08-24 mock 演练 + 金手指源码走读结论，与 flow-media-manager v1.25.1 同步）
> 1. 🚨 **长 URL 复制保真风险实锤**：`#s=` 内联深链上千字符，复制转手丢/错一个字符 gzip 整体报废（mock 实测：解码失败 → 页面空白；浏览器里则显示 localStorage 旧草稿）
> 2. **校验闸门**：URL 只复制一次进 shell 变量，同一条 Bash 命令「python 解码校验 → 通过才 open」；报错重调 `open_config`；模板与规则见 flow-media-manager《灌入金手指》章节（两边共用）
> 3. ~~**recommendedLink 一律走 Bash open**，不走 present_files 传 URL~~ 🚫 **已于 v2.29.0 作废**（金手指页面只走 present_files 右栏，绝不 Bash open）——历史记录，勿照做
> 4. ✅ **修正早前误判**：#3 落库缺表**不影响**内联预填（纯客户端解码）；"页面空白/旧数据"排查顺序：①URL 校验 → ②无痕窗口/清 localStorage

> **v2.26.0 改动**（相对 v2.25.0）：⭐ 产出 1 加 MCP 灌入——立项单文字产出后立即 `open_config` 灌 `{offerName, demand}` 拉起金手指「需求确认页」（用户决策，配套字段规范「两次灌入调用时序」章节）
> 1. **v2.4"只打对话栏"规矩推翻（部分）**：立项单**仍是对话栏文字**（评估输入锚点 + 用户确认依据 + 审计记录，三重价值保留），但不再是唯一形态——产出后立即调金手指 `open_config` 灌入 Part 1 需求字段（16 个，`demand.groups` 结构），拉起「需求确认页」，用户在页面上核对/改字段
> 2. **灌入策略**：两次灌入 = 两个独立页面（用户决策，不做会话续灌）——本环节只灌 `{offerName, demand}`；Part 2/3/4 策略字段由 flow-media-manager v1.25.0 在确认单环节全量灌入（48 字段）
> 3. **30 分钟过期**：`recommendedLink` 有效期 30 分钟（`expiresInMinutes` 返回值）——打开时提醒用户尽快点开；过期说一声重灌即可
> 4. **重灌规则**：用户在页面核对后回对话改答案 → 迭代产出新立项单文字后**重新调 `open_config`**（新 session 新链接，旧的自然过期，无脏页面）
> 5. **fallback**：金手指 MCP 不可用 → 跳过灌入，立项单文字照常产出（老链路），不阻塞流程
> 6. 全链路 4 步变 3 步：收尾句"下一步（：2/4 …）"改"下一步（：2/3 …）"；配套 `flow-media-manager` v1.25.0 / `flow-demand-to-media` v3.17.0

> **v2.25.0 改动**（相对 v2.24.0）：⭐ 可行性评估对齐新问卷口径与 og 判定链（C10/C14/兜底策略）
> 1. **五维打分依据重写两维**：ROI 可测算度 → 归因与数据基础（C14 开发资源三档 × C10 资产归因档位；无归因=点击 og + CPC 兜底、后端 ROI 测不出）；链路就绪度 → 线上门面就绪度（按 C10 多选资产清单逐项核 + C2=商品销售叠加 C11 商品渠道）
> 2. **版位匹配度补一句**：已勾选资产优先匹配（勾了微信小店 → 优先朋友圈+视频号小店链路）
> 3. **段 A 加第 5 推导维度**：承接/归因 = C14 × C10（标签云 4→5 个，加「承接：…」标签）
> 4. **段 C 风险示例替换**：「没有落地页」→「暂无线上门面（C10 全不勾）」——影响补"只能点击 og + CPC 兜底，做不了转化优化"，缓解补"零开发先搭小店/官方落地页（平台自动归因、无需代码）"
> 5. **段 E 补归因类建议示例**、**段 F 补一条坑**（无归因别选转化 og/oCPX——oCPX 要归因上报，没归因接不进去，只能点击 og + CPC 兜底）
> 6. **模板 footer 落款更名**：齐亮（腾讯广告竞价投放专家）→ 齐亮（金手指·媒介经理），对齐执行确认单 footNote 口径；模板段 A 标签云 4→5 个占位
> 7. 配套专家包 v3.52.0（v3.51.0 已被并行改动"删 C1 同步"占用）

> **v2.24.0 改动**（相对 v2.23.8）：⭐ 删除 C1「产品类型」题（与 C2 重复）+ 可行性评估单 C1 引用同步
>
> 1. **删 C1**（用户判读 + 知识库确认）：C1 四选项（推广商品/品牌活动/应用小游戏/收集销售线索）就是 C2 营销目的五枚举的 OPC 化翻版——官方营销四元组（promotion_product_basic.md）里"推广产品"是建单时从商品库**点选的具体对象**，不是提问层的东西。题数 14→13（第 1 批 4→3 题），其余题号保留不变（避免 C11/C14 交叉引用断链）
> 2. **品牌宣传路由改单信号**：原「C1 选推广品牌活动 × C3 选声量优先」双信号 → **C3 选"品牌声量优先"单独承载**品牌宣传营销目的
> 3. **可行性评估单 3 处同步**：头部元数据「广告类型」取值源 C1→C2 营销目的；段A 画像「产品」标签推导 C1 产品类型→C2 营销目的×C10 线上门面；段B 维度5 版位匹配度的产品形态判定源 C1→C2
> 4. **下游 og 判定链同步**（flow-media-manager v1.24.7 + og-library.md + 字段规范）：营销目的输入「C1×C2」→「C2（C3 声量优先→品牌宣传）」；自建载体细分推导 C1→C2（用户增长→APP/SDK，商品销售/线索留资→H5·小程序/JS）

> **v2.23.8 改动**（相对 v2.23.7）：⭐ C14 去第 4 选项
> 1. **去掉"不懂帮我判断"**：C14 只保留 3 个真实档位（专业开发团队/轻量开发资源/零开发基础）——三档已覆盖所有情况（用户必须选一个），"不懂"在开发资源问题上不是合理状态（公司规模常识题）
> 2. 配套专家包 v3.48.0

> **v2.23.7 改动**（相对 v2.23.6）：⭐ C14 description 回到白话（v2.23.6 过矫正纠正）
> 1. **选项 description 白话化**（用户反馈"选项需要白话"）：v2.23.6 用专业术语（JS 代码部署/SDK 接入/API 对接/平台自动归因路径）——过矫正。改为 OPC 生意话（"啥对接都能做/简单对接可以做/只能平台自带不能代码对接"）
> 2. **技术信息只留映射列**：归因档位、SDK/JS/API 等只进 og 判定链/映射列注记，不进选项文案
> 3. 配合 v2.23.2 定稿口径（label 精确、description 白话）一致
> 4. 配套专家包 v3.47.0

> **v2.23.6 改动**（相对 v2.23.5）：⭐ C14 定稿——用户题干 + 知识库专业术语选项
> 1. **题干用用户措辞**：「为了更好的效果，效果广告依赖您一定的开发资源提供。您当前团队的开发资源状况为？」（v2.23.5 自拟题干被用户判"不专业"，废弃）
> 2. **选项 description 换知识库专业术语**（attribution_basic.md）：JS 代码部署 / SDK 接入 / API 对接 / 深度归因配置 / JS 上报 / 平台自动归因路径——零开发档直接给专业建议（走微信小店/官方落地页，无需代码对接）
> 3. 映射列注记术语出处："不接归因=不能用优化目标投放"
> 4. 配套专家包 v3.46.0

> **v2.23.5 改动**（相对 v2.23.4）：⭐ C14 题干加背景 + 选项 paraphrase 定稿
> 1. **题干加"为什么需要开发"背景**（用户要求，简单说）：「统计广告带来的下单和留资，需要在您的页面里嵌一段腾讯追踪代码。公司里有人能做这个对接吗？」——OPC 先懂"为什么问开发"，再答"有没有人"
> 2. **选项 paraphrase 定稿**（用户给的三档措辞抛光）：专业开发团队（深度对接都拿得下）/ 轻量开发资源（外包/兼职/懂点技术，简单对接）/ 零开发基础（没人碰代码，全靠平台自带能力）/ 不懂帮我判断
> 3. **三档对应归因能力**（映射列注记）：专业=SDK/API 深度对接 / 轻量=浅层 JS / 零开发=平台自动归因路径——og 判定链输入
> 4. 配套专家包 v3.46.0

> **v2.23.4 改动**（相对 v2.23.3）：⭐ C14 同样按 v2.23.2 定稿口径修正
> 1. **C14 题主白话化**（用户截图反馈"公司有开发资源吗？"看不懂）：改为「**公司里有人能做技术对接吗？**」——v2.23.2 定稿口径一致（题干白话、label 精确、description 解释）
> 2. **description 同步加 JS/SDK/API 白话解释**：让 OPC 知道"技术对接"具体指什么
> 3. 选项 label 保持精确（"有开发团队/有外包兼职/没有开发/不懂帮我判断"）——不因 OPC 场景牺牲精确性
> 4. 历史 changelog 标题保留旧表述（"公司有开发资源吗？"）不回改；注记追加 v2.23.4 变更
> 5. 配套专家包 v3.45.0

> **v2.23.3 改动**（相对 v2.23.2）：⭐ C10 选项去 description
> 1. **C10 四选项不带 description**（用户决策）：label 已是精确专业术语（微信小店/视频号小店、腾讯官方落地页、企微承接、自建H5/小程序/APP）自明，白话解释冗余——调用 AskUserQuestion 时只传 label
> 2. **全问卷唯一例外**：其余题目仍遵守"每个选项必须带 description"通则（v2.3 立的规矩）；C10 例外已加显式注记
> 3. 配套专家包 v3.44.0

> **v2.23.2 改动**（相对 v2.23.1）：⭐ C10 口径定稿——题干白话、label 精确
> 1. **v2.23.1 白话化过矫正**：把选项 label 也白话了（"企微承接"→"企业微信"、"自建H5/小程序/APP"→"自己的网站/小程序/APP"、"腾讯官方落地页"→"腾讯广告落地页"）——用户纠正：**选项本身需要精确，只是问题需要改**
> 2. **定稿口径**：**题干**用 OPC 生意话（"客户看到广告后，能在哪里找到您或下单？"）；**label** 用精确专业术语（微信小店/视频号小店、腾讯官方落地页、企微承接、自建H5/小程序/APP）；**白话解释只放 description**
> 3. 此口径同步为全问卷通则：任何题的 label 都是精确术语，不因 OPC 场景牺牲精确性
> 4. 配套专家包 v3.43.0

> **v2.23.1 改动**（相对 v2.23.0）：⭐ C10 改多选资产清单 + OPC 白话化
> 1. **单选改多选**（用户洞察：四选项非并列——企微是转化目标非载体、自建是开发归属维度）——多选清单式提问消解并列问题；og 判定改为 **C2 营销目的 × 勾选资产**联合路由
> 2. **OPC 白话化**（用户反馈"承接配置看不懂"）：问题改为「客户看到广告后，能在哪里找到您或下单？（可多选）」；header"转化载体"→"线上门面"；"企微承接"→"企业微信"（客服或客户群，客户加了慢慢聊）；"自建H5/小程序/APP"→"自己的网站/小程序/APP"；**归因档位等技术信息只进映射列，不进选项文案**
> 3. 调用时设 `multiSelect: true`；题数不变 14 道 4 批；配套专家包 v3.42.0

> **v2.23.0 改动**（相对 v2.22.1）：⭐ C10 载体选项摊开（不加题）
> 1. **C10 四选项摊开，不再聚合**（用户决策）：微信小店/视频号小店（平台自动归因，卖货首选）、腾讯官方落地页（平台自动归因，留资/咨询通用）、**企微承接**（加微/入群私域沉淀，v2.22.0 曾删独立选项、v2.23.0 恢复）、自建H5/小程序/APP（需技术对接）
> 2. **"不太清楚"让位转自由输入**（沿用 C4 先例），问题文本尾部加引导句
> 3. **自建细分不加题**（用户决策"一个问题搞定"）：H5/小程序/APP 的分流由 **C1 产品类型**推导——推广应用/小游戏→APP 走 SDK；商品/线索→H5/小程序走 JS
> 4. 题数不变：仍 14 道 4 批；配套专家包 v3.41.0

> **v2.22.1 改动**（相对 v2.22.0）：⭐ C2 投放目标 label 格式修正
> 1. **官方枚举放到 label 首位**：v2.22.0 把 官方枚举 放描述尾部括号（`直接下单成交 — 看到广告就买（商品销售）`），UI 渲染时被截断，看不到枚举。改为 label 首位「`枚举·动作`」格式：`商品销售·直接下单成交` / `线索留资·留电话表单` / `加粉互动·加企微或关注公众号视频号` / `用户增长·下载或注册 APP 小游戏`——四项在选项卡一目了然，对齐 官方四元组 og 判定链的「营销目的」槽位
> 2. 描述顺序同步：白话解释放「— 」之后
> 3. 配套专家包 media-placement-expert v3.40.0 同步

> **v2.22.0 改动**（相对 v2.21.0）：⭐ 提需提问按官方四元组（营销目的→推广产品→营销载体→优化目标）重规范，对齐下游 og 判定链（flow-media-manager v1.24.0）
> 1. **C2 重写**：选项对齐官方营销目的五枚举——直接下单成交（商品销售）/ 留电话表单（线索留资）/ 加企微关注（加粉互动）/ 下载注册 APP 小游戏（用户增长）。**品牌宣传不占选项**（4 选项上限）：C1 选"推广品牌活动" + C3 选"声量优先"即路由
> 2. **C10 重写（载体题，og 判定关键解锁）**：旧"已经准备好了哪些"（多选混素材/落地页/企微）改为单选"用户看到广告后，主要在哪里完成转化？"——微信小店/腾讯官方落地页（均平台自动归因，无需开发）/ 自建 H5 小程序 APP（需技术对接）/ 自由输入兜底。**企微不再做独立选项**：加粉信号走 C2，企微入口走官方落地页咨询组件
> 3. **C11 新增（条件题）**："您要推广的商品目前有哪些渠道在售？"——仅 C2 选"直接下单成交"才问。已上架微信小店（转化 og 全开放）/ 外部店铺天猫京东抖音（可投引流但腾讯拿不到归因）/ 线下批销暂无线上 / 不太清楚
> 4. **C12 拆出（素材准备）**：从旧 C10 拆出独立问"手上已有哪些广告素材"（图文视频都有/有一些但不齐/都没有/帮我判断）
> 5. **批次重排**：3 批 12 题 → 4 批 14 题（第 3 批承接与资产 4 题 + 第 4 批历史与资源 2 题）
> 6. **立项单模板同步**：【四、素材与承接】新增"转化载体""商品渠道"两行；速查表、结构化交接表同步；`demand_carrier`/`demand_product_channel` 两个新字段
> 7. 配套 flow-media-manager v1.24.0

> **v2.21.0 改动**（相对 v2.20.0）：⭐ 新增 C12「开发资源」提问——投放优化目标（og）判定的前置依赖
> 1. **新增第 12 道选择题**（第 3 批第 4 题）：**公司有开发资源吗？**——映射 `demand_dev_resource`，选项：有开发团队（能做技术对接、事件上报）/ 有外包兼职（简单对接可以）/ 没有开发（用平台自带能力）/ 不懂帮我判断
> 2. **为什么问**：下游 flow-media-manager 段A 需要产出「优化目标 og」双字段——**有开发**（可对接转化归因上报）才能用深度转化 og（转化/ROI）；**无开发**只能选浅层 og（点击等，无需技术对接）。没有这个回答，og 判不了
> 3. **同步落点**：第 3 批表（3 题→4 题）、速查表（11 题→12 题 + C12 行）、立项单文字模板【四、素材与承接】新增"开发资源"行、show_widget 卡片模板同步、结构化交接表"素材与承接"分区加 `demand_dev_resource`
> 4. 配套 flow-media-manager v1.23.0

> **v2.20.0 改动**（相对 v2.19.0）：⭐ 产出 1 改两步法——`show_widget` 卡片展示规范 + 对话栏文字输出清单（plugin v3.35.0）
> 1. **用户明确诉求**：每次到"产出 1：需求立项单"时**固定用 show_widget 可视化插件**展示输出格式规范（让用户对照看"清单要这么写"）
> 2. **两步法**：
>    - **第 1 步**：`show_widget(loading_messages, title, widget_code)` 渲染"输出格式规范"卡片（HTML 片段：模板示例 + 6 条字符级参考习惯）
>    - **第 2 步**：按 v2.19.0 对话栏文字模板输出**实际清单**（v2.4 立的规矩保持：不变 Write/不 present_files/不卡片）
> 3. **v2.1.2 教训防护**：专家 subagent 环境 show_widget 工具可能不存在 → 加 fallback（工具异常时**跳过第 1 步** + 在对话里说明"已降级"+ **绝对不要谎报卡片已弹**）
> 4. **widget_code 模板**写进 SKILL.md（CSS + HTML 完整片段），模型照抄即可——标题含 📋 表情与项目名，徽章显示 `ad-demand-helper-pro v2.20.0` + `plugin v3.35.0`，底部 6 条参考习惯用网格卡片
> 5. **结构性硬要求** + **字符级软参考**分层：每到产出 1 必须尝试调 show_widget（不能换 markdown bullet），但卡片内部字符可灵活变通
> 6. **升级路径**：`show_widget` 工具在新版 WorkBuddy 是否已修复（subagent 环境可用）未验证——**先用 v2.20.0 跑一次全链路观察**，若 subagent 调用失败频繁，可降级为"主对话卡片 + subagent 跳过卡片"
> 7. 配套 plugin v3.35.0

> **v2.19.0 改动**（相对 v2.18.0）：⭐ 字符级"硬规则"改"参考习惯"（plugin v3.34.0）
> 1. **用户反馈**：v2.18.0 的"字符级硬规则（每条 0/1 判分，错一条就重写）"太细，模型过度纠结导致**输出效率差**
> 2. **改为**：模板照旧（**逐字模板仍保留**，照抄即可），但格式规范从"9 条字符级硬规则（🚨 强约束）"改为"6 条参考习惯（按用户截图实测）"
> 3. **唯一硬性约束只剩 2 条**：
>    - **结构**（5 分区 + 字段 + 收尾句）—— 不达成输出会失味
>    - **不用 md 任何语法**（表格/加粗/井号/分割线）—— 失味
> 4. **字符级细节**（缩进几个空格、4 字标签对齐方式、全角半角具体用法）—— 允许模型根据上下文微调，避免过度约束
> 5. **删除** 9 条 0/1 判分规则、"v3.10 旧模板错误已修"反向提醒、🚨 强约束标记
> 6. 配套 plugin v3.34.0

> **v2.18.0 改动**（相对 v2.17.0）：⭐ 需求清单文字输出格式标准化（plugin v3.33.0）
> 1. **格式实测标准化**：用户给的两张截图（8-21 / 8-22）成为唯一标准模板，**逐字模板**写入 SKILL.md，模型**照抄**而不是"参考"
> 2. **字符级硬规则**（每条 0/1 判分）：
>    - 标题：📋 + 半角空格 + "投放需求清单" + 半角空格 + **全角中点 U+00B7** + 半角空格 + 项目名（**项目名不带前缀**）
>    - 分隔线：**20 个 ASCII `-`**，顶部+底部各一条
>    - 字段：**2 个全角空格缩进**（不是 4 半角）
>    - 分隔：标签 + **全角冒号「：」** + 冒号后**无空格**直接接值
>    - 标签对齐：4 字以内用全角空格 `　` 补齐到 5 字宽（`总预算　：` 对齐 `投放周期：`）；4 字以上不补
>    - 分区：`【一、xxx】` 中文六角括号 + **顿号** + 独占一行 + 与字段块空一行
> 3. **删除错误规则**：v3.10 写的"4 字以内用全角空格补齐到 5 字宽"保留但**不冲突**；v3.10 模板里的"项目：　"+"日期：　"是**错的**（截图里冒号后无空格），已修正
> 4. **明确新增字段位置**：可加「六、xxx」等新区块（结构完全一致——独立标题 + 2 全角空格缩进 + 全角冒号 + 字段），便于以后加字段不用改模板
> 5. **禁用半角符号**清单扩充：半角冒号 `:` / 半角中点 `·` / 半角括号 `[]()` / 半角破折号 全部禁用，**所有标点必须用全角**
> 6. **项目/日期永远显示**（即使项目名 = offerId 同名也要显示，便于用户对照日期确认是哪次需求）
> 7. 配套 plugin v3.33.0

> **v2.17.0 改动**（相对 v2.16.0）：⭐ 提需中途规则类问题走知识库查询（plugin v3.28.0）
> 1. **新增《提需中途用户问规则/操作类问题》章节**：用户答题时冒出"我这行业要什么资质""被拒怎么申诉""落地页怎么建"等规则问题 → 调 `tencent-ads-delivery-guide` **查准了答**，不凭印象敷衍
> 2. **答完必须回流**：说清回到中断的那道题，别查完资料就把提需流程丢了
> 3. **身份一致性**：不输出那个 skill 的开场自我介绍；不标内部文件名（说"按腾讯广告的规定"而非"依据 references/xxx.md"）
> 4. 配套 `flow-demand-to-media` v3.14.0 / agents md 模式 D / plugin v3.28.0

> **v2.16.0 改动**（相对 v2.15.0）：⭐ 回退静默 HTML，改走结构化字段（plugin v3.27.0）
> 1. 🚨 **删除 v2.15 的"需求单静默 HTML"**：不再 `Write` 任何需求单文件——需求单**只有对话栏文字一个形态**（回到 v2.14 的干净状态）
> 2. **原因**：金手指配置页前端**本就支持结构化通道**（`demand.groups`），此前"生成 HTML → 页面再用 DOM 选择器解析回结构"是无谓往返，且依赖 `.sec/.field/.k/.v` 选择器——我们模板改样式就可能解析失败
> 3. **改为**：4/4 时由 flow-media-manager 直接把需求字段拼成 `demand.groups` 对象传入（实测 `success:true` / `degraded:false`，页面正常预填）
> 4. **新增《结构化交接（给 4/4 用）》章节**：明确 5 个分区与 `demand_*` 字段的对应关系，供 4/4 拼装
> 5. 少写一个文件、少一层解析风险、字段直给不会解析错
> 6. 配套 `flow-media-manager` v1.16.0 / `flow-demand-to-media` v3.13.0 / plugin v3.27.0

> **v2.15.0 改动**（相对 v2.14.0）：需求单增加"静默 HTML"形态（⚠️ **已被 v2.16.0 废弃**——改走结构化 `demand.groups`）

> **v2.14.0 改动**（相对 v2.13.1）：⭐ HTML 顶部加金手指 logo（plugin v3.25.0）
> 1. **`references/feasibility-template.md` topbar 左侧改为金手指 logo**：图标 base64 内嵌（用户提供的蓝色版原图裁切）+ "金手指 / AI · Ad Agent" HTML 文字 + 竖线分隔，替换原来的 📊 emoji
> 2. **新增 `references/gf-logo-base64.txt`**（base64 文本，写 HTML 时读它替换 `{{GF_LOGO_BASE64}}` 占位符）+ `gf-logo-icon.png`（源图留档）。⚠️ 不许自己画 SVG、不许引本地路径——HTML 必须自包含
> 3. **topbar 排版修正**（用户实测反馈"下面的文字排版不太行"）：`.topmeta` 宽屏右对齐 + 窄屏（≤760px）靠左加细线分隔，**不再居中错位**
> 4. 与《投放执行确认单》同款 logo 与 topbar（UI 统一）；配套 `flow-media-manager` v1.14.0 / plugin v3.25.0

> **v2.13 改动**（相对 v2.12.0）：统一可行性评估与执行确认单的 UI
> 1. ⭐ **新建 `references/feasibility-template.md` 完整 HTML 模板**（用户实测反馈"可行性评估和投放执行确认单的 UI 不一致，统一一下"）——根因：确认单有 html-memo-template.md 对照而评估没有，模型每次自由发挥导致样式漂移
> 2. **模板与执行确认单共用同一套设计系统**（同款 CSS 变量/字体/底色/白卡/topbar/章节卡），生成时复制模板只替换 {{占位符}}，CSS 原样保留
> 3. **特有组件**：总判断 hero（大数字总分 + 结论 badge + 策略基调）、五维 dim 卡（分数 + CSS 进度条 + 怎么算/白话讲/怎么提升）、风险清单、同赛道参考卡、建议/避坑列表
> 4. **此模板无"带偏"风险**（区别于已删的立项单模板）：可行性评估产物本来就是 HTML 文件，模板与形态一致；立项单是对话文字才不能有模板
> 5. 配套 plugin v3.18.0

> **v2.12 改动**（相对 v2.11.1）：文本格式硬约束
> 1. ⭐ **按 agent md 新文本格式要求重写评估收尾话术**：总分 → ⚠️缺什么（列表）→ 💡怎么补（指向评估）→ 后果白话
> 2. 配套 flow-media-manager v1.10.0 / plugin v3.17.0



> **v2.11.1 改动**（相对 v2.11.0）：C2 问题文案调整（用户反馈）
> 1. **C2 问题文案**：用户看到广告后，最希望他做什么？ → 您的投放目标是什么？。header 从「用户动作」改为「投放目标」保持语义一致。选项保持不变
> 2. 配套 plugin v3.16.0

> **v2.11 改动**（相对 v2.10.0）：产物收尾按受众分层
> 1. **可行性评估收尾话术重写**（用户视角四要素）：总分/结论 → **你现在最缺的是 XXX** → 怎么补（指向评估"优化建议"）→ 补不齐的后果（白话）——用户始终知道自己的缺口和改进路径
> 2. 配套 `flow-media-manager` v1.9.0 / plugin v3.15.0

> **v2.10 改动**（相对 v2.9.0）：进度条每轮显示 + 话术去标签化
> 1. ⭐ **进度条改为每条回复的最开头固定显示**（用户实测反馈：要"在所有对话的最开头出现"）——不再是"切步骤时才说"，是每轮都带
> 2. **产物收尾话术去标签化**（用户实测反馈：""这是什么：xxx 给谁用：xxx"看起来太奇怪"）——改为自然的一两句人话，讲清这东西讲了什么、用户重点看哪、接下来干嘛
> 3. 配套 flow-media-manager v1.8.0 / plugin v3.14.0

> **v2.9 改动**（相对 v2.8.1）：恢复原版丰富度（用户提供原版包对照）
> 1. ⭐ **可行性评估恢复完整 7 段结构**：v2.3 删"可视化分析"时误把五维 0-10 分制一并删掉，页面只剩 3 段骨架。现恢复：顶部总判断（可投/条件性可投/先别投）+ 五维总分 X/50 + 策略基调；段 B 五维 0-10 分制（预算充足度/ROI可测算度/合规就绪度/链路就绪度/腾讯广告版位匹配度，每维 CSS 进度条 + 怎么算/白话讲/怎么提升）；段 C 主要风险清单（❌ 带影响+缓解）；段 D 同赛道参考（3 案例）；段 E 优化建议；段 F 避坑清单。UI 与原版一致（CSS 进度条，非 canvas 图表）
> 2. **提问恢复原版问题与选项**：C1-C11 的问题措辞、选项 label 全部恢复原版（"您推广的产品类型是？""推广我的商品"等）；保留 3 批串行机制 + 每选项 description
> 3. 原"6 维度状态表"并入五维打分（素材→链路就绪度、测试周期→预算充足度、KPI 可达性→ROI 可测算度）
> 4. 交接摘要报总分：'总分 [X]/50，结论是[可投/条件性可投/先别投]'
> 5. 配套 flow-demand-to-media v3.10.0 / plugin v3.13.0

> **v2.8 改动**（相对 v2.7）：白话命名 + 模板升级 + 产出物说明
> 1. ⭐ **白话命名**（用户实测反馈"立项单/评估"是内部讲法）："需求立项单"→"**投放需求清单**"、"可行性评估"→"**《投放可行性评估》**"
> 2. **需求清单模板升级**：顶部加 `📋` 图标 + `───` 分隔线；分区标题用 `【一、xxx】`；字段对齐用全角空格
> 3. **每份产物后附一两句自然收尾话术**（讲清这东西是什么、用户重点看哪、接下来干嘛；禁用"这是什么：/给谁用："标签格式）
> 4. **4 步进度条话术**：1/4 → 2/4 → 3/4 → 4/4
> 5. 配套 `flow-demand-to-media` v3.9.0 / `flow-media-manager` v1.7.0 / plugin v3.11.0

> **v2.7 改动**（相对 v2.6）：修实测 bug——立项单仍被写成 HTML
> 1. 🚨 **删除 `references/demand-charter-template.md`**：实测发现明文规定"对话栏文字"后，模型读了模板文件仍按模板写 `需求立项单-xxx.html` 并推卡片。模板的存在本身就是诱导，删掉才断根
> 2. 产出 1 开头加 🚨 红牌警告：立项单不是文件、不 Write、不 present_files、不找模板
> 3. 配套 `flow-demand-to-media` v3.8.0 / `flow-media-manager` v1.6.0 / plugin v3.10.0

> **v2.6 改动**（相对 v2.5）：呈现方式按规范修正
> 1. ⭐ **从"只走 present_files"改为一次定型双轨**：依据《WorkBuddy 专家开发规范》4.2——正式专家环境系统分配的工具集里**没有 present_files**（只有 Read/Write/Grep/Glob/Bash/WebSearch/WebFetch 等），主对话环境才有。v2.5 在专家环境会让所有 HTML 退化成贴路径
> 2. **规则**：首次产出 HTML 时试调一次 `present_files`——成功则本会话全走它（右侧栏卡片）；失败/不存在则本会话全走 Bash `open`（浏览器）。一次定型，之后不再重试、不摇摆
> 3. **产物目录迁移**：从 `~/投放需求/` 挪到**工作区相对路径** `投放产出/[YYYY-MM-DD]/`——专家环境下用户在文件面板就能找到产物；Write 后记录绝对路径传给下游
> 4. 话术按实际轨道说：present_files 成功说"在右边栏打开了"，Bash open 说"在浏览器里打开了"
> 5. 配套 `flow-demand-to-media` v3.7.0 / `flow-media-manager` v1.5.0

> **v2.5 改动**（相对 v2.4）：HTML 呈现方式收敛为单一路径（⚠️ 已由 v2.6 修正——专家环境无 present_files，必须保留 Bash open 轨道）
> 1. ⭐ **可行性评估 HTML 统一走 `present_files` 产物 + 右侧栏打开**：`Write` → `present_files`，就这一条路
> 2. ❌ **删除 Bash `open` 兜底方案和"先试 A 再 fallback B"的探测逻辑**——模型不再需要判断环境/试探工具存在性，减少一次无谓的失败调用和摇摆
> 3. 话术统一为"**在右边栏打开了**"（不说"浏览器里打开了"——它是右侧栏产物，不是外部浏览器）
> 4. 配套 `flow-demand-to-media` v3.6.0 / `flow-media-manager` v1.4.0
> **v2.4 改动**（相对 v2.3）：立项单改为对话栏文字输出
> 1. ⭐ **需求立项单从「HTML 文件 + present_files 卡片」改为「对话栏文字」**：不 `Write`、不 `present_files`、不用 `demand-charter-template.md`。理由：立项单只是给用户快速确认用的，开文件弹卡片太重，文字直接可读可改
> 2. **明确不是 Markdown**：不用 `|` 表格、不用 `#` 井号标题、不用 `**加粗**`、不用 `---` 分割线。就是分区标题（一、二、三）+ 每行「字段名：值」
> 3. **可行性评估不受影响**：仍是 HTML 文件 + 自动打开预览，`present_files` 现在只传 1 个路径
> 4. **下游取值方式变更**：flow-media-manager 不再读立项单文件，改从对话上下文取 `demand_*` 值（文字立项单就在同一段对话里）
> 5. **迭代时重新完整输出**立项单文字，不要只说"已更新 X 字段"
> 6. 配套 `flow-demand-to-media` v3.5.0 / `flow-media-manager` v1.3.0


> **v2.3 改动**（相对 v2.2）：修 4 个用户实测 bug
> 1. 🚨 **选择题改为严格串行**：v2.2 写的"3 次 AskUserQuestion 在**同一条消息**里完成"是**错误表述**——并行发出时用户只能答第一组，后两组被界面直接判「已跳过」，11 题里 8 题答案全丢。现明确：**一条消息只能有 1 个 AskUserQuestion 调用**，答完一批再发下一批
> 2. **每个选项补 `description`**：v2.2 只列了选项 label，用户点选时看不到备注说明。现三张题表都按 `label — description` 给全，白话解释放 description，不塞进 label
> 3. **可行性评估废弃 `show_widget`**：改回和立项单一样的独立 HTML + 自动打开预览（`present_files` 一次传 2 个路径，无该工具则 Bash `open`）。原因：`show_widget` 与 flow-demand-to-media v3.2.0+ 方案冲突，且专家 subagent 环境没有这个工具 → 用户看不到任何卡片产出
> 4. **删除「可视化分析」段**：可行性评估 HTML 不再加 Chart.js 雷达图 / 气泡矩阵 / 任何图表——用户明确反馈"没有必要"
> 5. 配套 `flow-demand-to-media` v3.4.0


> **v2.2 改动**（相对 v2.1.2）：
> 1. ⭐ **按"问过没问过"决定显隐**：11 题 + 4 填空**没问到**的项 → 整行隐藏，不写"—（待定）""缺失"；**问过的照常展示**，包括用户答"没有/都没有/否"（这是有效信息，说明要从零准备）
> 2. **该推导的继续推导**：选"不懂帮我判断"或能从已有答案推出的（行业推合规、画像推 TA、卖点推简介）照推，推出来就展示；推不出才隐藏
> 3. 分区内全部隐藏 → 整个分区也不输出，序号顺延
> 4. 立项单去掉"必填/可选"标记列，footer 不做「必填 X/Y 校验」
> 5. 默认值不再硬填（`demand_explore_crowd` / `demand_special_material` 不再默认"否"，因为压根没问）


> **v2.1.2 改动**（相对 v2.1.0）：⚠️ 第 2 点已由 v2.3 废弃
> 1. 可行性评估清单**仍写 HTML 文件**（供下游 flow-media-manager 读路径，链路不断）
> 2. ~~对话内呈现方式从 `present_files` 推卡片改为 `show_widget` 渲染~~ → **v2.3 已废弃回滚**，改回 present_files/Bash open
> 3. 立项单产出方式不变（HTML 文件 + present_files 推卡片）
> 4. 文件路径仍传给下游 flow-media-manager（链路不断）


> **v2.1 改动要点**（相对 v2.0）：
> 1. 加 4 项前置填空：Offer名 / 行业 / 卖点 / 优势——基础信息必须用户说
> 2. ~~12 道选择题**一轮问完**，不分两段~~ → **v2.3 已修正为 3 批串行**（并行会丢答案）
> 3. KPI/成本预期用**大白话表述**，不甩 CPA/ROI 术语
> 4. 成本预期改为绝对金额（"30元以内"），不按客单价比例推导
