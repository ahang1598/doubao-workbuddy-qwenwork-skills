# 模型与价格说明

AI-HIVE 提供 100+ 模型能力，具体可用模型、路由和价格会随平台配置变化。

## 实时读取规则

1. 按任务类型调用 `list_models`。
2. 只展示本次返回的候选模型和 `routingModes`。
3. 选定路由后，使用同一模型 `pricingSnapshot` 中对应记录估算费用。
4. 价格展示要注明计费单位、批量、时长或 token 等依据；无法从返回值确定时明确说明。
5. 创建付费任务时原样传回该快照，最终费用以服务端结算为准。

营销材料可以使用“100+ 模型”等平台能力表达，但执行层不得据此编造当前可用模型、价格或路由。
