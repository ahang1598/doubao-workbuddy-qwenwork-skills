[project]
name = "xhs-content-reader"
version = "1.0.0"
description = "小红书内容读取子技能 — 通过 Chrome CDP 协议提取笔记正文和评论"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28",
    "websockets>=12.0",
]

[project.scripts]
xhs-reader = "scripts.cli:main"
