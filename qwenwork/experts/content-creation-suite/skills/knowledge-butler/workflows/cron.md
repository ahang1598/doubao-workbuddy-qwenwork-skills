# 定时任务（被动响应）

> 本文件按需加载——仅在用户询问或设置定时任务时 Read。

**不主动推销定时任务**——只在用户主动询问或触发时创建。

## 创建

用户说"帮我创建每日自动同步听记的定时任务"时，用 `cron_use` 工具创建（每天 08:00，workflow 5 + workflow 6）。

## 被动提示

首次完成 workflow 5 时末尾可放一行被动提示：

```
💡 想让它每天自动同步？随时告诉我"开启每日自动同步"。
```
