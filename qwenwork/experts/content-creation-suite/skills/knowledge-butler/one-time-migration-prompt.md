# 一次性迁移提示词（发给悟空执行）

> 复制下面分隔线之间的内容，发给悟空

---

帮我做一次知识库结构升级。改动涉及 4 类文件，请严格按以下步骤执行，每改完一类文件先告诉我进度再继续下一类。

## 1. 升级全部专题文件（23 个）

逐个处理 `$KB_ROOT/2-AI知识库/专题/` 下的每个 `.md` 文件：

**a) 删除 frontmatter 中的 `lifecycle_stage` 行**

改前：
```yaml
entity_kind: person
lifecycle_stage: L1
created: 2026-05-17
```

改后：
```yaml
entity_kind: person
created: 2026-05-17
```

**b) 删除 `## 事件线` 整个段落**（包括段标题和下面的所有条目），保留 `## 被提到` 段不动。

注意：有的文件事件线标题是 `## 事件线（append-only · 精简索引）`，有的可能是 `## 事件线`，都要删。删除范围是从 `## 事件线` 开头一直到下一个 `##` 段标题之前的所有内容（含空行）。

**c) 如果有 `## L2 扩展段` 标题，改名为 `## 扩展段`**

⚠️ **引用检查**：事件线中的 `[[摘要链接]]` 在被提到段中都有对应记录，删除事件线不会丢失引用关系。删每个文件前先确认这一点。

完成后告诉我：处理了多少个文件，各文件删了多少条事件线。

## 2. 升级目录.md

文件路径：`$KB_ROOT/3-AI工作记录/目录.md`

**a) 素材摘要区改表头 + 补"涉及专题"列**

改前：
```
| 日期 | 标题 | 来源 |
```

改后：
```
| 日期 | 标题 | 来源 | 涉及专题 |
```

对每一行已有条目，Read 对应的摘要文件，找出它涉及的主要专题（3-5 个），填入"涉及专题"列，用 `/` 分隔。例如：`孟岩/内容偏好/知识飞轮三态模型`

**b) 专题区改表头：删"阶段"列 + 补"一句话"列**

改前：
```
| 名称 | 类型 | 阶段 | 最后更新 |
```

改后：
```
| 名称 | 类型 | 最后更新 | 一句话 |
```

对每个专题，Read 专题文件，用 ≤15 字概括填入"一句话"列。

**c) 排序：全部改为时间正序**（最早在上，最新在下）

当前素材摘要是最新在上面，需要反转为最早在上面。专题区按"最后更新"正序排列。

完成后告诉我进度。

## 3. 升级规则手册.md

文件路径：`$KB_ROOT/规则手册.md`

**a) 删除"三层生命周期"小节**（从 `### 三层生命周期` 到 `---` 分隔线之前的所有内容）

**b) 重写"整合判断规则"章节**

把现有的 L1/L2 分层描述替换为：

```markdown
## 专题更新规则

> 每次新素材进来，AI 按段落类型决定怎么更新专题页。

| 段 | 更新规则 |
|---|---|
| **主体段** | 有认知增量才重写。自问：①新素材改变了理解吗？②推翻/修正了现有描述吗？③不改会错过重要变化吗？三问都"否"→ 不动 |
| **被提到** | 每次都追加，不存在"什么都不动"的情况 |
| **信号清单**（仅部分专题有） | 新模式 → 加条目；已有模式的新实例 → 追加子条目；无新信号 → 不动 |
| **扩展段**（引述/矛盾/开放问题） | 按需添加，不设门槛 |
```

**c) 删除专题页规则中对"阶段"字段的引用**——不再区分 L1/L2 阶段。

完成后告诉我进度。

## 4. 操作记录.md 排序修正

文件路径：`$KB_ROOT/3-AI工作记录/操作记录.md`

把所有条目按时间戳正序排列（最早在上，最新在下）。只改顺序，不改内容——操作记录是历史日志，内容中的"L1"、"事件线"等词是当时的事实记录，保持原样。

完成后告诉我进度。

## 验证清单

全部完成后，逐条检查：

1. `grep -r "lifecycle_stage" 2-AI知识库/专题/` → 应该无结果
2. `grep -r "事件线" 2-AI知识库/专题/` → 应该无结果
3. 目录.md 素材摘要区有 4 列（日期/标题/来源/涉及专题）
4. 目录.md 专题区有 4 列（名称/类型/最后更新/一句话），无"阶段"列
5. 目录.md 素材摘要第一行是最早的条目，最后一行是最新的
6. 操作记录.md 第一个条目时间最早，最后一个条目时间最新
7. 规则手册.md 中不含"L1"、"L2"、"事件线"
8. 所有专题文件中的 `[[双链]]` 仍能在目录.md 中找到对应条目

报告验证结果。

---
