# CHANGELOG

## v4.2 — 0811 真机测试整改（2026-08-11）

依据 QwenWork 真机测试轮（速查卡场景，普通/高级模型两次执行）整改 3 项：

1. **速评判定规则交叉歧义**（references/output-quick-card.md）：增加判定顺序（高风险→需关注→正常，任一高级别命中即取高级别），歧义时取保守评级；「正常」增加"无诉讼记录"条件，消除与「需关注」的条件交叉
2. **§3.0 风险概览工具匹配过窄**：匹配标准放宽为 description 含 risk_overview/风险概览/风险汇总/聚合统计/aggregation summary，或返回 schema 含失信/被执行/经营异常计数的聚合统计工具
3. **§1.2 探测关键词补齐**：新增风险概览/风险汇总/风险总览/聚合/risk overview/risk summary/aggregation，与 dimension-tool-map.md 辅助能力关键词对齐

## v4.1 — 交付门禁 7 项 P0 修复（2026-08-10）

对 `scripts/validate_dd_report.py` 做对抗性复核，发现并修复 7 项 P0 缺陷（漏拦 / 误拦 / 哨兵绕过 / 自证放行），全部经夹具实测（md + docx）。

### 修复

1. **空壳放行**：全篇各章节均为「数据缺失」的 13 章报告此前被放行 → 新增全篇空壳检查：剔除缺失标注与表格标签后无任何章节含实质信息即 exit 1
2. **必需维度缺失仍出评级**：工商登记（章二）/ 股权结构（章四）/ 主要人员（章六）/ 知识产权（章七）/ 行业资质（章八）/ 风险评估（章九）整体无实质内容时此前仍放行综合评级 → 任一必需维度无实质内容即 exit 1
3. **模板行为**：`references/report-template.md` 此前被拦截但报错为通用空壳/留空/占位符项，反向教 agent 填模板 → 新增模板黑名单（文件名含 report-template 或内容含模板占位符/模板注释），显式拦截并提示「模板文件，非报告」
4. **误拦合规降级摘要**：带 ⚠️ 公开信息摘要声明的摘要此前被拦死且报错引导补成完整报告 → 摘要形态单独校验（⚠️ 强制免责声明 + 实质内容 + 无评级结论 + 无供应商名），合规即 exit 0；报错信息不再指导补成完整 13 章
5. **头部来源误判**：来源行简短/未提及 MCP 此前被误判为降级而拦死合规报告 → 降级判定只认「⚠️ 公开信息摘要」声明与「工商数据获取状态：未取得」声明行，不依据来源行长句
6. **哨兵绕过**：「天 眼 查」「**企查查**」等写法此前可绕过供应商哨兵 → 匹配前归一化（去 markdown 标记 / 去空白 / 全半角转换）
7. **自证放行**：声明「已通过 WebSearch 取得」即放行完整 13 章 → 新增形态一致性校验：完整 13 章 ⇄ 头部「主要数据来源」声明连接器数据源（能力语义）；WebSearch-only ⇄ 必须走 ⚠️ 公开信息摘要形态；不一致 exit 1

## v4.0 — 去供应商硬编码 + 交付门禁（千问办公连接器口径）(2026-08-10)

按项目「三铁律」（只写能力语义 / 三段式探测 / 分级降级）改造，全目录清除数据供应商硬编码。

### 新增

- `references/connector-setup.md`：三类能力语义（主体与工商登记 / 司法与经营风险 / 经营扩展）、在「千问办公 设置 → 连接器」启用方式、未启用后果与能力自检清单
- `scripts/validate_dd_report.py`：尽调报告交付门禁（退出码 0 通过 / 1 拦截 / 2 输入错误，支持 md + docx），硬拦截 ① 降级态假装完整报告 ② 13 章缺章 ③ 章节空壳 ④ 报告出现供应商名（防改造回退哨兵）⑤ 缺失字段留空未写 `数据缺失`
- SKILL.md §1.1 所需能力语义清单（10 项，分必需 / 重要 / 快速模式必需三档）
- SKILL.md §1.5 能力覆盖表（记录本次运行探测到的能力，不记供应商）
- SKILL.md §6.2 脚本门禁段：交付前必须运行门禁脚本，未通过禁止交付
- report-template.md 头部新增必填声明行 `**工商数据获取状态：** 已取得 / 未取得`

### 变更

- frontmatter description：删除双供应商表述与供应商触发词，补入企业信用 / 工商登记 / 股权结构 / 涉诉 / 被执行 / 企业画像等能力语义关键词（保持单行）
- SKILL.md §1 整章重写：`Provider 选择与可用性` → `数据源能力探测`（能力语义清单 → 探测 → 匹配 → 调用 → 能力覆盖表 → A 档降级）
- `references/dimension-tool-map.md`：按供应商分列的 server / 工具映射 → 「维度 → 能力语义 → 探测关键词 → 必需返回字段」，维度定义与业务含义全部保留
- §3.3 历史维度降级、§6.1 数据来源标注、异常处理表、状态说明、参考文件表：全部改为能力语义口径
- 报告模板 / 速查卡 / 专项模板：来源标注统一为「企业工商信息查询连接器（运行时探测）」
- 状态说明：删除数据源偏好的跨会话持久化，改为每次运行重新探测

### 删除

- `references/` 下两份供应商专属安装文档（合并为 `connector-setup.md`）
- §1.4 安装引导整段：agent 读写连接器配置文件并注入用户凭据的流程（千问办公下由用户在平台侧启用连接器，技能不碰配置与凭据）
- 数据源偏好机制（原 memory 持久化 + 双列决策矩阵 + 「换某家」切换指令）

---

## v3.0 — 公开信息尽调预检报告升级 (2026-05-21)

基于与专业律所真实尽调报告（德恒杭州所投资尽调）的对比分析，全面升级报告质量和专业性。

### 新增

- **WebSearch 补充数据层**：从"降级兜底"提升为与 MCP 并行的第二数据层，按条件触发裁判文书网、基金业协会、行业监管网站、信用中国等公开来源的深度搜索
- **分析处理步骤**（第五步）：新增跨维度风险交叉分析（5个模式）、法律严重性判断、经营范围-资质匹配检查
- `references/websearch-sources.md`：WebSearch 搜索策略、触发条件、频次上限、来源标注规范
- `references/analysis-framework.md`：法律分析框架（风险严重性矩阵、交叉模式定义、待核实事项触发规则、经营范围-资质匹配表、评级汇总规则）
- 报告新增 5 个章节：工商变更历史梳理、关联企业图谱深度分析、知识产权、行业资质与许可证、待线下核实事项清单
- `output-single-dimension.md` 新增 4 个专项模板：工商变更历史、知识产权、行业资质、关联企业
- 执行摘要升级：关键发现 + 分维度风险评级 + 建议后续调查重点
- `dimension-tool-map.md` 新增：工商变更历史（核心维度）、关联企业深度（核心维度）、WebSearch 补充层、分析层

### 变更

- 报告定位：从「企业法律尽职调查报告」→「企业公开信息尽职调查预检报告」
- 工作流：从 4 步扩展为 6 步（新增第四步 WebSearch 补充 + 第五步分析处理）
- 报告结构：从 9 章扩展为 13 章
- 风险评估章节：从扁平列表重构为分类分析（含法律分析 + 建议）
- 股权结构章节：新增实际控制人认定路径、股权质押/冻结、出资到位分析
- 主要人员章节：新增交叉任职分析、人员变更历史、人员风险
- 免责条款：明确标注不替代律师线下核查
- TodoWrite 规划门：新增 WebSearch 补充、交叉分析、待核实事项生成为必要任务
- 自审循环：新增综合报告须确认分析步骤已完成

### 设计决策

- WebSearch 为补充非替代，MCP 仍为主数据源
- 法律分析规则外置于 `analysis-framework.md`，避免 SKILL.md 过长
- 关联企业递归上限 2 层 / 20 实体
- WebSearch 单次报告上限 10 次调用
- DOCX 脚本无需修改（基于 Markdown 解析，新章节自动兼容）

---

## v2.2 — 5 项使用反馈整改 (2026-05-20)

针对实际使用中暴露的 5 类问题，补强执行层约束与交付质量。

### 新增

- `scripts/generate_docx.py`：Markdown → DOCX 转换脚本（python-docx），内置统一排版规范（宋体/黑体、A4、1.5 行距）
- `requirements.txt`：python-docx 依赖声明
- SKILL.md §3.2「执行前 TodoWrite 计划」门：强制 agent 把维度列为待办，逐一标记完成
- SKILL.md §4.2「自审循环」门：任何核心维度 pending 时禁止输出报告
- SKILL.md §1.5「声明纯化」：WebSearch 降级严禁出现天眼查/企查查字样
- SKILL.md description 新增 `TRIGGER when` 场景描述（合同对方、被告/原告、投融资标的等）

### 变更

- §1.4 安装引导：从"手动粘贴"改为"agent 自动写入 `.mcp/mcpServerConfig.json` → 失败再手动粘贴"两层
- §4.2 DOCX 路径：从"docx skill → Markdown"两档改为"py 脚本 → docx skill → Markdown"三档
- §4.3 格式规范：从原则性描述改为具体参数（字体/字号/行距/页边距），由脚本保证
- `references/install-tyc.md` / `install-qcc.md`：新增"自动安装"章节（首选），保留手动粘贴为兜底
- `references/report-template.md`：数据来源 + 免责条款改为 `{actual_sources}` 占位符，附填写约束注释
- SKILL.md description `TRIGGER keywords` 补充：合同对方、合作伙伴、投融资标的、上下游

### 解决的问题

1. 维度漏调导致报告章节空白 → TodoWrite 计划 + 自审循环双门禁
2. WebSearch 模式免责声明误标天眼查/企查查 → 声明纯化约束
3. DOCX 生成慢/格式乱 → 预置 py 脚本，强制使用
4. MCP 安装流程低效 → agent 自动写入配置文件
5. 触发条件覆盖不足 → TRIGGER when 场景化描述

---

## v2.1 — 结构优化（静态评测整改）(2026-05-20)

针对静态评测 CP15/CP16/CP17/CP20 的整改。

### 新增

- `references/install-tyc.md` / `references/install-qcc.md`：MCP JSON 与 Key 说明从主文件拆出
- `references/dimension-tool-map.md`：维度 → server / 工具描述关键词映射（执行层单点维护）
- SKILL.md §4.2「输出完成标准」：综合/专项必填项、缺失字段口径、Markdown 降级路径

### 变更

- SKILL.md 瘦身（约 267 → ~200 行）：路由层（§1–2）与执行层（§3–4）分节，安装与维度表改为 reference 指针
- 工作流程概览表：明确四阶段职责边界

### 说明

- 未新增 `scripts/`：MCP 调用仍由 Agent 在运行时发起；维度映射沉淀为 `dimension-tool-map.md` 以降低维护漂移（与 v2 设计「不写死工具名」一致）

---

## v2.0 — lawd-company-info-tyc-qcc (2026-05-20)

从 `lawd-company-info` (仅天眼查) 升级为双数据源版本。

### 新增

- **企查查 (QCC) 支持**: 新增 6 个 server 的完整配置 (qcc-company / qcc-risk / qcc-ipr / qcc-operation / qcc-executive / qcc-history)
- **Provider 选择与偏好记忆**: 用户首次使用选择 TYC 或 QCC,通过代理 memory 系统跨会话持久化,可随时切换
- **决策矩阵**: 基于 memory 偏好 + 实际可用性自动决策,覆盖 9 种场景
- **维度映射三列表**: 每个维度同时标注 TYC 和 QCC 对应的 server 来源
- **qcc-history 优雅降级**: 未认证时跳过历史维度,不阻断主流程
- **数据来源标注**: 所有输出必须在头部标注实际使用的 provider

### 变更 (相比 v1)

- `name` 字段: `lawd-company-info` → `lawd-company-info-tyc-qcc`
- `description`: 新增"企查查"触发关键词
- 第一步: 从"MCP 可用性检测"重构为"Provider 选择 + 可用性检测 + 偏好记忆"
- 第三步: 入口工具从仅 TYC 扩展为 TYC/QCC 双路径
- 维度表: 从单列(应查字段)扩展为三列(字段 + TYC 来源 + QCC 来源)
- 状态说明: 从"无状态"改为"除 provider 偏好外无状态"
- report-template.md: 数据来源行支持双 provider 标注

### 删除

- `scripts/` 目录 (v1 中的占位 .js 脚本,已在 v1 优化中删除)

### 保持不变

- 6 核心维度 + 5 按需维度 + 辅助工具的整体分层
- 输出模板 (output-single-dimension.md / report-template.md)
- 第二步需求识别、第四步交付流程
- 文档格式规范
- 公开信息降级路径

---

## v1.1 — lawd-company-info (2026-05-20)

针对 MCP 部分的优化 (仅天眼查版本)。

### 变更

- 服务器名修正: `tianyancha` → `tyc-mcp`
- 接入方式修正: SSE → streamable-http
- MCP 检测: 从"try-catch 探测"改为"直接看工具列表"
- 安装引导: 给出精确可粘贴的 settings.json 片段
- 维度表: 不写死具体工具名,LLM 运行时按 description 匹配
- 降级路径: MCP 不可用时只出公开信息摘要,不出完整报告
- description: 精简到 138 字符,只保留触发条件
- 无需重启客户端

### 删除

- `scripts/` 目录 (三个占位 .js 文件)
